# Emergency: Prod Backend 404 全局排查

> 2026-07-30 用户从 IDE 真机 log 反馈:`/api/resume/current`、`/api/ai/assist-field`、`/api/match` 都 404
> 本文件是 SSH 排查 + 修复 runbook。

## 🎯 已知情况

- 我们已 push 3 个 commit 到 main:`c23861b` (merge) + `bdea66a` (CI config 注入 + smoke) + `1efdece` (admin applications)
- GH Actions 应已自动 deploy,**但** IDE 实测时:
  - trycloudflare 隧道 `lion-grass-pillow-hebrew.trycloudflare.com` 200 OK (健康)
  - 生产 `43.139.176.199:443` 200 OK (健康)
  - 但是核心路由 `/api/match` `/api/ai/assist-field` `/api/jobpilot/*` 全 404
  - `/api/auth/login` 200 (有路由) / `/api/resume/current` 401 (有路由但要登录) / `/api/match` 401 (有路由但要登录)

**结论**: trycloudflare → 转生产 — 生产 backend 跑的是**老版本**(没有 jobpilot/ai 路由)

## 🔍 Phase 1: SSH 进去看 backend 到底是哪个 commit

```bash
ssh ubuntu@43.139.176.199

# 1. PM2 list 看进程
pm2 jlist | python3 -c "import sys,json; [print(p['name'], '| cwd:', p['pm2_env']['pm_cwd'], '| uptime:', p['pm2_env'].get('pm_uptime','?'), '| restarts:', p['pm2_env'].get('restart_time','?')) for p in json.loads(sys.stdin.read())]"

# 期望有 resume-app-backend 进程
```

```bash
# 2. 进 backend 目录看是哪个 commit
cd /opt/resume-app/backend
git log -1 --oneline
git status -sb
```

**期望**:
- git log → `1efdece` (admin applications) 或更新
- git status → clean, ahead 0

**如果不匹配** → backend 是个老 bundle / 别的目录 → 看 ProcessWorkingDirectory in pm2

## 🔧 Phase 2: 三种情况 + 修复

### 情况 A: git log 不是 1efdece → deploy 没成功

```bash
cd /opt/resume-app
git pull origin main
cd backend
npm ci
# 确保 .env 还在
ls -la .env
pm2 restart resume-app-backend --update-env
```

### 情况 B: git log 是 1efdece 但 PM2 没 restart → 进程还在跑老代码

```bash
pm2 restart resume-app-backend --update-env
# 或更狠:
pm2 delete resume-app-backend
pm2 start ecosystem.config.js --only resume-app-backend
```

### 情况 C: PM2 进程不在 / 路径不对 → 它跑的不是真 backend

常见原因:某些脚本 `pm2 start src/index.js` 漏了 `cd /opt/resume-app/backend` → 跑的是 server-side 其他脚本

```bash
# 看实际进程
ps auxf | grep -E "node|pm2" | head -10

# 看 PM2 启动命令
pm2 jlist | python3 -c "import sys,json; p=json.loads(sys.stdin.read()); [print(pp['name'], pp['pm2_env']['exec_interpreter'], pp['pm2_env']['pm_exec_path']) for pp in p]"
```

## 🧪 Phase 3: 验证 backend 真有新路由

```bash
# 在 server 上 (跑 localhost:3003)
curl -s http://127.0.0.1:3003/api/health
# 200 OK

# 找一个新路由,老 backend 没有的
curl -s -X POST http://127.0.0.1:3003/api/jobpilot/profile-diagnose \
  -H "Content-Type: application/json" \
  -d '{"education":"x","aiAbility":"x","projects":"x","target":"x","timeline":"x"}'

# 期望 401 (没 token, 但路由存在)
# 如果 404 → backend 还跑老代码
```

## 🗄 Phase 4: 跑 migration (别忘了)

```bash
mysqldump -h 127.0.0.1 -uroot -p'$MYSQL_ROOT_PASSWORD' resume_app \
  > /tmp/before-jobpilot-migration-$(date +%Y%m%d-%H%M%S).sql

mysql -h 127.0.0.1 -uroot -p'$MYSQL_ROOT_PASSWORD' resume_app \
  < /opt/resume-app/backend/db/migrations/jobpilot.sql

# 验证:
mysql -h 127.0.0.1 -uroot -p'$MYSQL_ROOT_PASSWORD' resume_app -e "
  SHOW COLUMNS FROM resumes LIKE 'story_points';
  SHOW COLUMNS FROM jobs LIKE 'verify_status';
  SHOW TABLES LIKE 'jobpilot_applications';
"
```

## ⚠️ 关于 trycloudflare tunnel

`lion-grass-pillow-hebrew.trycloudflare.com` 看名字像是 `cloudflared tunnel --url https://43.139.176.199:443` 起的临时 tunnel。

**问题**:
- 这是临时 tunnel,主机一重启就断
- 密码 `D:\小程序密钥.key` 在 CI 注入的是 `https://43.139.176.199:443`,但 IDE 本地 `src/config.js` 还指向 trycloudflare

**如果 tunnel 死了**:
- IDE 全 5 步流 API 全 404 (因为指向死 URL)
- 修:在本地 IDE 编辑 `mini-program/src/config.js`,把 apiBaseUrl 改成 `https://43.139.176.199:443` (要勾 "不校验合法域名")
- 或者重新跑 cloudflared tunnel (推荐用 trycloudflare 但要常驻)

## 📋 TL;DR 给用户

**最关键 3 行命令**(按顺序跑,看输出决定下一步):

```bash
ssh ubuntu@43.139.176.199 'pm2 jlist | head -50; cd /opt/resume-app/backend 2>&1; git log -1 --oneline'
```

期望看到:`resume-app-backend` 进程 + git log = `1efdece`

如果不对,跑:
```bash
ssh ubuntu@43.139.176.199 'cd /opt/resume-app && git pull origin main && cd backend && pm2 restart resume-app-backend --update-env && sleep 2 && curl -sk -X POST http://127.0.0.1:3003/api/jobpilot/profile-diagnose -H "Content-Type: application/json" -d "{}" -w "\n[HTTP %{http_code}]\n"'
```

期望最后一行:`[HTTP 401]` (路由 OK 但没 auth)

## 🆘 完全卡住怎么办?

把命令输出贴给我,我根据实际情况 fix。

---

_Generated by Claude · 2026-07-30 · 配套 commit 1efdece_
