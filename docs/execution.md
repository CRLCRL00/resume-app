# 执行步骤 — 简历推荐小程序

> 最后更新：2026-06-27
> 总周期：~32 工作日（含 3 天缓冲）

## 阶段速览

| 阶段 | 名称 | 工作日 | 关键依赖 | 必做核心项 | 阶段进度% 目标 |
|------|------|--------|----------|------------|---------------|
| 0 | 基建 | 3 | — | 服务器/账号/备案启动 | 100% |
| 1 | 后端骨架 | 5 | 阶段 0 | 建表 + 登录 + LLM 连通测试 | 100% |
| 1.5 | 缓冲/验收 | 1 | 阶段 1 | 后端骨架验收 + 修缺 | 100% |
| 2 | 小程序骨架 | 4 | 阶段 1 | 登录 + 表单 + 简历页骨架 | 100% |
| 3 | 简历生成 | 4 | 阶段 2 | 表单提交→LLM→预览 端到端 | 100% |
| 4 | 岗位管理端 | 4 | 阶段 3 | CRUD + Prompt + 日志 | 100% |
| 5 | 匹配核心 | 6 | 阶段 3 并行 | 粗筛+缓存+LLM+列表+详情 | 100% |
| 5.5 | 缓冲/验收 | 1 | 阶段 5 | 匹配核心验收 + 修缺 | 100% |
| 6 | 上线打磨 | 3 | 阶段 4、5 | 安全加固+备份+备案对接 | 100% |
| 7 | 缓冲 | 1 | 阶段 6 | 应对不可控 | 100% |
| 8 | 微信审核 | 1-3 | 阶段 7 | 提交审核 | 100% |

> **总周期：~32 工作日**（缓冲从原 3 天拆为 1+1+1=3 天，分布到阶段 1 后、阶段 5 后、阶段 7）

## 阶段 0 — 基建（3 天）

> 拆 3 子任务推进，每子任务各 1 天。

### 子任务 0.1 — 环境搭建

- [ ] 注册微信小程序账号，获取 AppID
- [ ] 注册腾讯云账号
- [ ] 购买轻量应用服务器 2C4G
- [ ] 注册域名（.cn）
- [ ] **启动 ICP 备案**（与开发并行，7-20 天，前置必做）
- [ ] 注册 DeepSeek 账号、获取 API Key、充值 ¥10
- [ ] 申请 COS 桶（用于备份）

### 子任务 0.2 — 代码仓库 + 分支规范

- [ ] `git init` 初始化仓库
- [ ] 写 `.gitignore`（含 `.env`、`node_modules/`、`.superpowers/` 等）
- [ ] 建分支模型：`main`（稳定）/ `develop`（集成）/ `feature/*`（功能）/ `hotfix/*`（修复）
- [ ] 配置 GitHub / Gitee 远程仓库
- [ ] 首次 commit 推 `main` + `develop`

### 子任务 0.3 — 服务器初始化 + 文档基线

- [ ] 服务器装 Node 20、MySQL 8、Redis 6、Nginx、PM2、certbot
- [ ] 验证：`node -v`、`mysql --version`、`redis-cli ping`、`nginx -v`、`pm2 -v`
- [ ] 写 `docs/operations/` 目录的运维手册骨架（部署 / 备份 / 监控）

### 可选

- [ ] 准备图标 / Logo 占位
- [ ] 写项目 README 完善版

### 验收量化

| 指标 | 目标 |
|------|------|
| 服务器 SSH 可达 | 100% |
| 基础软件装齐 | 5/5 全部能跑通 |
| 备案受理号 | 已拿到 |
| Git 仓库 | 已建好 + 首次 commit 推送 |
| 文档基线 | `docs/` 全部文档已存在且结构完整 |

---

## 阶段 1 — 后端骨架（5 天）

### 必做

- [ ] 初始化 Node 工程（Express + 目录结构 + ESLint）
- [ ] `.env` / `.env.example` / `.gitignore`
- [ ] PM2 配置 + 启动脚本
- [ ] Nginx 反代 + SSL（开发期可用 IP 自签证书）
- [ ] **建表 SQL**：users / resumes / jobs / matches / admins / prompts / admin_operation_logs
- [ ] **种子数据 SQL**：20+ 岗位 + 默认 prompt 模板
- [ ] `POST /api/auth/login`（code2session）
- [ ] 用户鉴权中间件 + 管理员鉴权中间件
- [ ] `GET /api/health`
- [ ] **`GET /api/test/llm`**（验证 DeepSeek 链路）

### 可选

- [ ] winston 结构化日志
- [ ] joi 参数校验中间件

### 验收量化

| 指标 | 目标 |
|------|------|
| 核心接口覆盖率 | 100%（auth / health / test-llm / admin-check） |
| 接口单元测试 | ≥ 6 个 case 全过（含 401 / 403 / 500 / 业务正常） |
| 错误日志 | 关键路径有 winston 日志输出 |
| 数据库表 | 7 张表全部创建，索引完整 |
| 种子数据 | 岗位 ≥ 20 条 + 2 个默认 prompt |
| LLM 连通 | `/api/test/llm` 返 200，输出含 token 计数 |
| Nginx HTTPS | 自签证书下 TLS 握手成功 |
| 代码行数 | 后端代码 ≥ 500 行（含注释） |

---

## 阶段 1.5 — 缓冲 / 验收（1 天）

### 必做

- [ ] 阶段 1 所有「验收量化」指标复测
- [ ] 修复阶段 1 遗留的 P0/P1 bug
- [ ] 与 `main` 分支同步代码，确认 develop 集成无冲突
- [ ] 写阶段 1 总结到 devlog（踩坑 + 决策）

### 验收量化

| 指标 | 目标 |
|------|------|
| 阶段 1 验收全过 | 100% |
| P0 bug 遗留 | 0 |
| develop 分支 | 干净，无未提交代码 |

---

## 阶段 2 — 小程序骨架（4 天）

### 必做

- [ ] 小程序工程 init（绑定 AppID）
- [ ] `utils/request.js` 封装（自动带 token + 401 处理）
- [ ] `utils/storage.js` 草稿持久化
- [ ] 首页（动态按钮 + 静默登录）
- [ ] 资料表单（**先 3 步核心**：教育 + 经历 + 期望）
- [ ] 简历预览页（占位 Markdown 渲染）
- [ ] 岗位列表 / 详情页骨架（占位）

### 可选

- [ ] towxml 集成
- [ ] 异常页面通用样式

### 验收量化

| 指标 | 目标 |
|------|------|
| 页面完成度 | 4 个核心页（index / form / resume / jobs 占位） |
| 真机走通率 | 100%（首页 → 表单 → 简历预览 无报错） |
| 表单草稿 | 切换 step 数据保留，重新进入自动回填 |
| API 调用封装 | 100% 请求走 `utils/request.js` |
| 包体积 | 主包 < 1.5MB |

---

## 阶段 3 — 简历生成（4 天）

### 必做

- [ ] `POST /api/resume/generate`（同步调用 + 10s 超时 + 2 次重试）
- [ ] `max_tokens=1500` + JSON 输出约束
- [ ] 简历生成限流（3 次/天）
- [ ] 前端表单提交 → loading → 跳转预览 → 渲染 LLM 结果
- [ ] towxml 集成 Markdown 渲染
- [ ] 「换一版表述」按钮（30s 防抖 + 限流联动）

### 可选

- [ ] 简历版本对比
- [ ] 导出 Markdown 文件

### 验收量化

| 指标 | 目标 |
|------|------|
| 端到端跑通率 | 100%（表单提交 → LLM 返回 → 简历渲染） |
| 简历生成质量 | 抽样 3 份样本，≥ 2 份人工评「可投」 |
| Markdown 渲染 | 标题/列表/段落 100% 正常 |
| 限流有效 | 同一用户连点 4 次生成，第 4 次返 429 |
| 异常 case | LLM 超时 / 失败 → 友好提示，不白屏 |
| Token 消耗 | 单次生成 ≤ 1500 input + 1500 output |

---

## 阶段 4 — 岗位管理端（4 天）

### 必做

- [ ] 管理端分包配置
- [ ] 「我的」页面管理员入口 + 前端 `onLoad` 拦截
- [ ] 岗位列表 / 编辑 / 软删 / 上下架
- [ ] Prompt 读 / 写页（改完触发 `match:*` 缓存清理）
- [ ] `admin_operation_logs` 中间件记录关键操作
- [ ] 管理端限流（30 次/分钟）

### 可选

- [ ] Markdown 编辑器预览
- [ ] 批量导入岗位（CSV）

### 验收量化

| 指标 | 目标 |
|------|------|
| 岗位 CRUD | 5 个接口（list / create / update / delete / toggle-online）全过 |
| Prompt 读写 | 2 个接口 + 改完触发缓存清理 全过 |
| 双层鉴权 | 前端 onLoad 拦截 + 后端中间件 均生效 |
| 操作日志 | 关键操作 100% 落库（含 admin_openid、action、detail） |
| 管理端限流 | 30 次/分钟 触发后返 429 |
| 软删可恢复 | 已软删岗位可通过 `is_deleted=0` 恢复 |

---

## 阶段 5 — 匹配核心（6 天）

> 阶段 3 完成后即可启动，与阶段 4 并行。

### 必做

- [ ] 粗筛 SQL：城市 / 薪资 / 学历层级 / 工作年限 / 岗位名模糊 + 技能排序
- [ ] 列表级缓存 `match:list:{uid}:{rid}`
- [ ] 单岗位缓存 `match:{uid}:{jid}`
- [ ] `POST /api/match`（全流程编排 + 防重锁 + 日限流）
- [ ] LLM 精排（≤15 岗位 + JSON 强约束 + `max_tokens=1000`）
- [ ] LLM 失败兜底：返缓存 → 返技能命中数
- [ ] 前端匹配页（分阶段 loading + 35s 超时）
- [ ] 列表卡片（匹配分圆环 + 文字 + 理由）
- [ ] 岗位详情页（理由 + 描述 + 复制按钮）
- [ ] 缓存失效联动：资料变更 / prompt 改 / 岗位改，用 SCAN

### 可选

- [ ] 匹配历史可视化
- [ ] 订阅消息通知有新匹配

### 验收量化

| 指标 | 目标 |
|------|------|
| 端到端匹配 | 真机填资料 → 看到 ≤ 10 条匹配岗位 |
| 缓存命中 | 第二次进入列表 ≤ 200ms 返回 |
| 粗筛维度 | 5 个（城市/薪资/学历/经验/岗位名）+ 技能排序 全生效 |
| 匹配质量 | 抽样 5 份匹配结果，Top 3 人工评「值得点开」≥ 4 份 |
| LLM 精排 | 输入 ≤ 15 岗位，超限强制截断 |
| 异常 case | 粗筛 0 / 无简历 / LLM 全失败 / 缓存失效 4 种全过 |
| Token 消耗 | 单次精排 ≤ 1500 input + 1000 output |

---

## 阶段 5.5 — 缓冲 / 验收（1 天）

### 必做

- [ ] 阶段 5 所有「验收量化」指标复测
- [ ] 修复阶段 5 遗留的 P0/P1 bug
- [ ] 端到端联调：用户填资料 → 生成简历 → 匹配 → 详情 → 复制
- [ ] 写阶段 5 总结到 devlog（匹配效果调优笔记）

### 验收量化

| 指标 | 目标 |
|------|------|
| 阶段 5 验收全过 | 100% |
| P0 bug 遗留 | 0 |
| 端到端联调 | 1 遍完整通过 |

---

## 阶段 6 — 上线打磨（3 天）

### 必做

- [ ] `.env` 验证不入 git
- [ ] MySQL 低权限业务账号、Redis 密码
- [ ] 内存上限（MySQL 512M / Redis 256M）
- [ ] PM2 日志切割 + `pm2 reload` 验证
- [ ] `scripts/backup.sh` + crontab（每日 03:00 → COS）
- [ ] 备份 7 天清理
- [ ] Redis RDB 持久化
- [ ] certbot 自动续期
- [ ] README + 部署文档 + 运维手册
- [ ] **备案完成后**：域名解析、Nginx HTTPS、小程序后台域名白名单、真机正式环境验证

### 验收量化

| 指标 | 目标 |
|------|------|
| 安全基线 | 6 项全过（.env 入 gitignore / MySQL 低权限 / Redis 密码 / SQL 参数化 / XSS 转义 / 限流有效） |
| 内存约束 | MySQL ≤ 512M / Redis ≤ 256M / Node ≤ 500M 全部落实 |
| 备份真演练 | 从 COS 下载 → 还原 → 数据校验 1 遍通过 |
| 备份保留 | 7 天自动清理验证 |
| PM2 热重启 | `pm2 reload` 部署不停服验证 |
| certbot 续期 | `certbot renew --dry-run` 成功 |
| 真机生产 | 全流程 1 遍跑通（含真 HTTPS 域名） |
| 文档完整 | README + 部署 + 运维 三件套无歧义 |

---

## 阶段 7 — 缓冲期（3 天）

- 处理备案延迟、LLM 效果调优、未预见的 bug
- 不安排新功能，仅修复 / 优化

---

## 阶段 8 — 微信审核（1-3 天）

- 提交小程序审核
- 准备类目资质（工具类目通常免资质）
- 应对审核驳回

---

## 每阶段交付清单（统一模板）

每阶段结束必须产出：

- [ ] 该阶段的代码（已 commit + push）
- [ ] `devlog/YYYY-MM-DD.md` 当日开发日志
- [ ] 该阶段涉及文档的更新（README / 部署文档 / 接口文档）
- [ ] 下一阶段的启动清单