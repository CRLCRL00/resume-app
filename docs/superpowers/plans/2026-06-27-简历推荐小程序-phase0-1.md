# 简历推荐小程序 — 阶段 0 + 1 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭好开发基础设施（账号 / 服务器 / 仓库 / 文档），并交付可运行的 Node 后端骨架（建表 / 登录 / LLM 链路验证）。

**Architecture:** 单进程 Node 20 + Express 4 后端，MySQL 8 + Redis 6 同机部署，Nginx 反代 + 自签证书（备案期用 IP），PM2 进程管理。阶段 1 只交付 HTTP 接口骨架与连通性，业务逻辑在阶段 2-6 填。

**Tech Stack:**

- Node 20.x（LTS），Express 4.x
- mysql2（连接池 + Promise API）
- ioredis（Redis 6 客户端）
- axios（DeepSeek HTTP 调用）
- jsonwebtoken（自签 token）
- winston（结构化日志）
- dotenv（环境变量）
- joi（参数校验，阶段 1 可选）
- supertest + node:test（测试，零额外测试框架）
- PM2（进程管理）
- Nginx + 自签证书（备案期）
- DeepSeek API（`https://api.deepseek.com/v1/chat/completions`）

---

## 文件结构（阶段 0+1 产出）

```
简历app/
├── backend/                              # 后端工程根
│   ├── src/
│   │   ├── index.js                      # 启动入口（监听端口）
│   │   ├── app.js                        # Express app 工厂（导出供测试）
│   │   ├── config/
│   │   │   ├── index.js                  # dotenv 加载 + 读取 ENV
│   │   │   ├── db.js                     # MySQL 连接池
│   │   │   └── redis.js                  # Redis 客户端
│   │   ├── middleware/
│   │   │   ├── auth.js                   # 用户鉴权（解析 Bearer token）
│   │   │   ├── adminAuth.js              # 管理员鉴权（查 admins 表）
│   │   │   └── errorHandler.js           # 全局错误兜底
│   │   ├── routes/
│   │   │   ├── health.js                 # GET /api/health
│   │   │   ├── auth.js                   # POST /api/auth/login
│   │   │   ├── test.js                   # GET /api/test/llm
│   │   │   └── admin/
│   │   │       └── index.js              # /api/admin/* 占位（阶段 4 填）
│   │   ├── services/
│   │   │   ├── wechat.js                 # code2session 封装
│   │   │   ├── token.js                  # JWT 签发 / 校验
│   │   │   └── llm.js                    # DeepSeek 客户端
│   │   ├── utils/
│   │   │   └── logger.js                 # winston 实例
│   │   └── db/
│   │       ├── schema.sql                # 7 张表 DDL
│   │       └── seed.sql                  # 20 岗位 + 2 prompt + 1 管理员
│   ├── tests/
│   │   ├── helpers/
│   │   │   └── setup.js                  # 启动 app + 注入测试 DB
│   │   ├── health.test.js
│   │   ├── auth.test.js
│   │   └── test-llm.test.js
│   ├── .env.example                      # 环境变量样板（提交）
│   ├── .env                              # 本地实际值（不提交）
│   ├── .gitignore
│   ├── .eslintrc.json                    # 简单 ESLint 配置
│   ├── ecosystem.config.js               # PM2 配置
│   ├── package.json
│   └── README.md
├── deploy/
│   └── nginx/
│       └── resume-app.conf               # Nginx 站点配置（dev 模式 IP）
├── docs/
│   └── operations/
│       ├── deploy.md                     # 部署手册（阶段 0 骨架）
│       ├── backup.md                     # 备份手册（阶段 6 填）
│       └── monitoring.md                 # 监控手册（阶段 6 填）
├── scripts/                              # 已存在的脚本目录
└── README.md                             # 项目根 README
```

**职责边界：**

- `app.js` 纯工厂函数，不监听端口，便于测试注入
- `index.js` 只做 `app.listen()` + 优雅退出
- `config/` 只读 ENV，不做任何业务
- `services/` 纯函数 / 客户端封装，不碰 Express req/res
- `routes/` 只做参数提取 + 调 service + 返 JSON
- `middleware/` 只做鉴权 / 错误兜底
- `db/` 纯 SQL 文件，由 `npm run db:init` 触发

---

## TDD 节奏说明（给小白）

阶段 1 的代码任务都按 TDD 走，固定 5 步循环：

```
1. 写失败测试       → 在 tests/ 写一个 test() 块
2. 跑测试看红       → npm test，应该 FAIL
3. 写最小实现       → 在 src/ 写刚好让测试过的代码
4. 跑测试看绿       → npm test，应该 PASS
5. commit            → git add . && git commit -m "..."
```

每步 2-5 分钟。养成习惯：见到红色先高兴（说明测试在工作），见到绿色再 commit。

**TDD 三原则：**

1. **测试先于实现** — 没测试不写代码
2. **一次只改一个东西** — 不要边改边加新功能
3. **红了再写实现** — 如果测试没红就过了，说明测试没意义

---

## 阶段 0 — 基建（3 个工作日）

阶段 0 全部是手动操作（注册 / 购买 / 安装），无 TDD。每步末尾有「验收命令」，复制粘贴运行确认。

### 任务 0.1.1 — 注册微信小程序账号

**Files:** 无

**步骤：**

- [ ] **Step 1: 打开微信公众平台**

浏览器访问 https://mp.weixin.qq.com/ ，点「立即注册」→「小程序」。

- [ ] **Step 2: 填写主体信息**

选「个人」主体（个人无资质要求，最快），填邮箱（未注册过微信公众平台的）、密码、验证码。

- [ ] **Step 3: 邮箱激活 + 信息登记**

登录注册邮箱点激活链接。回到微信公众平台登录，扫码绑定管理员微信。填写个人姓名、身份证号、扫码验证。

- [ ] **Step 4: 拿 AppID**

左侧「开发」→「开发管理」→「开发设置」→ 复制 **AppID（小程序ID）** 到记事本。

- [ ] **Step 5: 验收**

记事本里有 AppID，格式如 `wx1234567890abcdef`。记下后填到 `.env` 的 `WX_APPID=`。

---

### 任务 0.1.2 — 注册腾讯云账号 + 买轻量服务器

**Files:** 无

**步骤：**

- [ ] **Step 1: 注册腾讯云**

访问 https://cloud.tencent.com/ ，用手机号注册 + 实名认证（个人，身份证 + 人脸）。

- [ ] **Step 2: 进入轻量应用服务器控制台**

控制台搜索「轻量应用服务器」→ 点「新建」。

- [ ] **Step 3: 选配置**

- 地域：离你最近的（如「广州」）
- 镜像：选「CentOS 7.9」或「Ubuntu 22.04」（二选一，下面命令对应 Ubuntu）
- 实例规格：**2 核 4GB**（套餐一般 60-80 元/月）
- 带宽：5Mbps 峰值
- 系统盘：50GB SSD（默认）
- 购买时长：1 个月（先短租，跑通再续）

- [ ] **Step 4: 设置 root 密码 + 付钱**

记录 IP（如 `123.45.67.89`）、root 密码、用户名（默认 root）。

- [ ] **Step 5: 验收**

记事本记录：

```
SERVER_IP=123.45.67.89
SERVER_USER=root
SERVER_PASSWORD=xxx
```

能 SSH 登录：`ssh root@123.45.67.89` 输入密码能进。

---

### 任务 0.1.3 — 注册域名 + 启动 ICP 备案

**Files:** 无

**步骤：**

- [ ] **Step 1: 买域名**

腾讯云控制台搜索「域名注册」，搜想要的 `.cn` 或 `.com`（`.cn` 便宜，¥10-30/年；个人无资质要求）。

- [ ] **Step 2: 域名实名认证**

买完会要求填身份证信息，1-3 个工作日审核。

- [ ] **Step 3: 提交 ICP 备案**

腾讯云控制台搜索「ICP 备案」→ 点「开始备案」→ 填主体信息（个人身份证 + 住址）→ 填网站信息（网站名称、用途说明）→ 上传身份证正反面 + 核验照（手机扫码拍）。

- [ ] **Step 4: 等初审**

腾讯云 1-3 个工作日初审，初审通过会寄背景布幕给你（拍照用）或要求 App 端视频核验。

- [ ] **Step 5: 验收**

记事本记录：

```
DOMAIN=resume.example.cn
ICP_STATUS=已提交（受理号：xxx）
```

备案状态在「ICP 备案」控制台可查，预计 7-20 天下证。**这步走完后不卡后面的开发**，继续往下推。

---

### 任务 0.1.4 — 注册 DeepSeek + 拿 API Key

**Files:** 无

**步骤：**

- [ ] **Step 1: 注册 DeepSeek**

访问 https://platform.deepseek.com/ ，手机号注册。

- [ ] **Step 2: 充值**

「充值」→ ¥10（足够几百次测试，prod 阶段 100 DAU 也才几块）。

- [ ] **Step 3: 创建 API Key**

「API Keys」→ 「创建新 Key」→ 命名 `resume-app-prod` → 复制 Key 到记事本（**只显示一次**）。

- [ ] **Step 4: 验收**

记事本记录：

```
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxx
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
```

---

### 任务 0.1.5 — 申请 COS 桶（备份用，阶段 6 配）

**Files:** 无

**步骤：**

- [ ] **Step 1: 进 COS 控制台**

腾讯云控制台搜索「对象存储 COS」→ 开通服务（个人免费额度够用）。

- [ ] **Step 2: 建桶**

「存储桶列表」→「创建存储桶」：

- 名称：`resume-app-backup-你的唯一后缀`（如 `resume-app-backup-2026`）
- 地域：跟服务器同地域
- 访问权限：**私有读写**
- 其他默认

- [ ] **Step 3: 验收**

记事本记录桶名 + 地域。阶段 6 备份脚本要用，**阶段 1 不动**。

---

### 任务 0.2.1 — git init + .gitignore

**Files:**

- Create: `d:\项目\简历app\.gitignore`

**步骤：**

- [ ] **Step 1: 初始化仓库**

在项目根 `d:\项目\简历app\` 下，git bash 运行：

```bash
cd /d/项目/简历app
git init -b main
```

预期：输出 `Initialized empty Git repository in ...` + `HEAD` 切到 `main`。

- [ ] **Step 2: 写 .gitignore**

`Write` 工具创建 `d:\项目\简历app\.gitignore`：

```gitignore
# 依赖
node_modules/
**/node_modules/

# 环境变量（敏感）
.env
.env.local
.env.*.local
!.env.example

# 日志
logs/
*.log
pm2-logs/

# 备份
backups/
*.sql.gz

# 编辑器
.vscode/
.idea/
*.swp
.DS_Store

# 系统
.superpowers/
Thumbs.db
```

- [ ] **Step 3: 验证 git 状态**

```bash
git status
```

预期：显示 `Untracked files: .gitignore`，没有任何其他文件被列出来（项目里的 `docs/` `devlog/` `scripts/` `README.md` 应该在下一步一起加）。

- [ ] **Step 4: commit**

```bash
git add .gitignore
git commit -m "chore: init git + .gitignore"
```

预期：1 file changed, X insertions。

---

### 任务 0.2.2 — 远程仓库 + 首次提交

**Files:** 无

**步骤：**

- [ ] **Step 1: 在 Gitee / GitHub 建空仓库**

Gitee（国内快）：https://gitee.com/new ，仓库名 `resume-app`，**不要**勾选「用 README 初始化」。

或 GitHub：https://github.com/new ，同上。

- [ ] **Step 2: 加远程 + 推 main**

复制仓库 URL（如 `https://gitee.com/你的名字/resume-app.git`），本地运行：

```bash
cd /d/项目/简历app
git remote add origin https://gitee.com/你的名字/resume-app.git
git push -u origin main
```

预期：提示输入用户名/密码（或 token），最终 `* [new branch] main -> main`。

- [ ] **Step 3: 建 develop + feature 分支**

```bash
git checkout -b develop
git push -u origin develop
```

预期：develop 分支推到远程。

- [ ] **Step 4: 验证**

打开 Gitee/GitHub 仓库页，能看到 `main` 和 `develop` 两个分支。

---

### 任务 0.3.1 — 服务器装 Node 20

**Files:** 无（服务器操作）

**步骤：**

- [ ] **Step 1: SSH 登录服务器**

本地 git bash：

```bash
ssh root@123.45.67.89
```

输入密码进。

- [ ] **Step 2: 装 Node 20（用 NVM）**

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc
nvm install 20
nvm use 20
nvm alias default 20
```

预期：`nvm install 20` 输出 `Now using node v20.x.x`。

- [ ] **Step 3: 验收**

```bash
node -v    # v20.x.x
npm -v     # 10.x.x
which node # /root/.nvm/versions/node/v20.x.x/bin/node
```

---

### 任务 0.3.2 — 服务器装 MySQL 8

**Files:** 无（服务器操作）

**步骤：**

- [ ] **Step 1: 装 MySQL**

```bash
apt update
apt install -y mysql-server
systemctl start mysql
systemctl enable mysql
```

预期：装完无报错。

- [ ] **Step 2: 初始化密码**

```bash
mysql_secure_installation
```

依次：

- VALIDATE PASSWORD：选 `No`（不强密码）
- Set root password：设一个复杂密码，记到记事本 `MYSQL_ROOT_PASSWORD=xxx`
- Remove anonymous users：`Yes`
- Disallow root login remotely：`Yes`（安全）
- Remove test database：`Yes`
- Reload privilege tables：`Yes`

- [ ] **Step 3: 改 root 允许 localhost 密码登录**

```bash
mysql -u root -p
# 输入密码
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '你的密码';
FLUSH PRIVILEGES;
EXIT;
```

- [ ] **Step 4: 建业务库（阶段 1 建表时再用）**

```bash
mysql -u root -p -e "CREATE DATABASE resume_app CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
```

- [ ] **Step 5: 验收**

```bash
mysql -u root -p -e "SHOW DATABASES;" | grep resume_app
```

预期：输出 `resume_app`。

---

### 任务 0.3.3 — 服务器装 Redis 6 + 密码

**Files:** 无（服务器操作）

**步骤：**

- [ ] **Step 1: 装 Redis**

```bash
apt install -y redis-server
systemctl start redis-server
systemctl enable redis-server
```

- [ ] **Step 2: 设密码**

```bash
redis-cli
# 进 redis 交互
> CONFIG SET requirepass "你的Redis密码"
> CONFIG REWRITE
> EXIT
```

记事本记录 `REDIS_PASSWORD=xxx`。

- [ ] **Step 3: 验证**

```bash
redis-cli -a "你的Redis密码" ping
```

预期：输出 `PONG`。

---

### 任务 0.3.4 — 服务器装 Nginx + certbot

**Files:** 无（服务器操作）

**步骤：**

- [ ] **Step 1: 装 Nginx**

```bash
apt install -y nginx
systemctl start nginx
systemctl enable nginx
```

- [ ] **Step 2: 装 certbot**

```bash
apt install -y certbot python3-certbot-nginx
```

- [ ] **Step 3: 验收**

```bash
nginx -v    # nginx version: nginx/1.18.x
certbot --version    # certbot 2.x.x
curl -I http://localhost    # HTTP/1.1 200 OK（nginx 默认页）
```

---

### 任务 0.3.5 — 服务器装 PM2

**Files:** 无（服务器操作）

**步骤：**

- [ ] **Step 1: 全局装 PM2**

```bash
npm install -g pm2
```

- [ ] **Step 2: 验收**

```bash
pm2 -v    # 5.x.x
```

---

### 任务 0.3.6 — 写运维手册骨架

**Files:**

- Create: `d:\项目\简历app\docs\operations\deploy.md`
- Create: `d:\项目\简历app\docs\operations\backup.md`
- Create: `d:\项目\简历app\docs\operations\monitoring.md`

**步骤：**

- [ ] **Step 1: 写 deploy.md 骨架**

`Write` 工具创建 `d:\项目\简历app\docs\operations\deploy.md`：

```markdown
# 部署手册

> 最后更新：YYYY-MM-DD
> 阶段 0 骨架，阶段 6 补完

## 服务器

- IP：填你的服务器 IP
- 系统：Ubuntu 22.04
- 用户：root

## 软件版本

| 软件 | 版本 | 用途 |
|------|------|------|
| Node | 20.x | 后端运行时 |
| MySQL | 8.x | 主存储 |
| Redis | 6.x | 缓存 + 限流 |
| Nginx | 1.18.x | 反代 + HTTPS |
| PM2 | 5.x | 进程管理 |

## 部署步骤

阶段 6 补完：上传代码 → `npm ci` → `pm2 reload` → 健康检查。

## 回滚

阶段 6 补完：保留上一版本代码目录 + 备份。
```

- [ ] **Step 2: 写 backup.md 骨架**

`Write` 工具创建 `d:\项目\简历app\docs\operations\backup.md`：

```markdown
# 备份手册

> 最后更新：YYYY-MM-DD
> 阶段 6 补完

## 备份内容

- MySQL 全量（每日）
- Redis RDB（每日）
- 上传 COS

## 保留策略

- 7 天

## 恢复演练

- 每月 1 次
- 阶段 6 部署后做第一次真演练
```

- [ ] **Step 3: 写 monitoring.md 骨架**

`Write` 工具创建 `d:\项目\简历app\docs\operations\monitoring.md`：

```markdown
# 监控手册

> 最后更新：YYYY-MM-DD
> 阶段 6 补完

## 健康检查

- `GET /api/health` — 后端
- `redis-cli ping` — Redis
- `mysqladmin ping` — MySQL

## 告警

阶段 6 补完：PM2 邮件告警 / 钉钉 webhook。
```

- [ ] **Step 4: commit**

```bash
cd /d/项目/简历app
git add docs/operations/
git commit -m "docs(operations): 0 阶段运维手册骨架"
git push origin develop
```

---

## 阶段 1 — 后端骨架（5 个工作日）

阶段 1 全是代码，严格 TDD。每任务 5 步循环。

### 任务 1.1 — 初始化 Node 工程

**Files:**

- Create: `d:\项目\简历app\backend\package.json`
- Create: `d:\项目\简历app\backend\.eslintrc.json`
- Create: `d:\项目\简历app\backend\README.md`

**步骤：**

- [ ] **Step 1: 写 package.json**

`Write` 工具创建 `d:\项目\简历app\backend\package.json`：

```json
{
  "name": "resume-app-backend",
  "version": "0.1.0",
  "private": true,
  "description": "简历推荐小程序后端",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test tests/",
    "test:watch": "node --test --watch tests/",
    "lint": "eslint src/ tests/",
    "db:init": "node scripts/db-init.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "joi": "^17.13.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.10.0",
    "winston": "^3.13.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "supertest": "^7.0.0"
  }
}
```

- [ ] **Step 2: 写 .eslintrc.json**

`Write` 工具创建 `d:\项目\简历app\backend\.eslintrc.json`：

```json
{
  "env": {
    "node": true,
    "es2022": true,
    "jest": false
  },
  "extends": "eslint:recommended",
  "parserOptions": {
    "ecmaVersion": 2022,
    "sourceType": "script"
  },
  "rules": {
    "no-unused-vars": ["warn", { "argsIgnorePattern": "^_" }],
    "no-console": "off"
  }
}
```

- [ ] **Step 3: 写 backend/README.md**

`Write` 工具创建 `d:\项目\简历app\backend\README.md`：

````markdown
# 后端

简历推荐小程序后端，Node 20 + Express 4。

## 本地跑

```bash
cp .env.example .env
# 改 .env 里的 DB/REDIS 密码
npm install
npm run db:init
npm run dev
```

## 跑测试

```bash
npm test
```

## 部署

见 `docs/operations/deploy.md`。
````

- [ ] **Step 4: 装依赖**

git bash 在 backend 目录：

```bash
cd /d/项目/简历app/backend
npm install
```

预期：装完无 ERR，`node_modules/` 出现。`package-lock.json` 自动生成。

- [ ] **Step 5: commit**

```bash
cd /d/项目/简历app
git add backend/package.json backend/package-lock.json backend/.eslintrc.json backend/README.md
git commit -m "feat(backend): init package + eslint + readme"
```

---

### 任务 1.2 — 写 .env.example + config loader

**Files:**

- Create: `d:\项目\简历app\backend\.env.example`
- Create: `d:\项目\简历app\backend\src\config\index.js`

**步骤：**

- [ ] **Step 1: 写 .env.example**

`Write` 工具创建 `d:\项目\简历app\backend\.env.example`：

```bash
# 服务
NODE_ENV=development
PORT=3000

# 微信小程序
WX_APPID=wx_your_appid
WX_SECRET=your_secret

# JWT
JWT_SECRET=change_me_to_a_long_random_string_at_least_32_chars
JWT_EXPIRES_IN=30d

# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=resume_app

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# DeepSeek
DEEPSEEK_API_KEY=sk_your_key
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# 日志
LOG_LEVEL=info
```

- [ ] **Step 2: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\config.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');

// 先把 .env 加载到 process.env
require('../src/config');

test('config exports env values', () => {
  const config = require('../src/config');
  assert.ok(config.PORT, 'PORT should be defined');
  assert.ok(config.DB_HOST, 'DB_HOST should be defined');
  assert.ok(config.JWT_SECRET, 'JWT_SECRET should be defined');
});

test('config validates required keys', () => {
  // 临时清空关键 key，应该抛错
  const orig = process.env.JWT_SECRET;
  delete process.env.JWT_SECRET;
  delete require.cache[require.resolve('../src/config')];
  assert.throws(() => require('../src/config'), /JWT_SECRET/);
  process.env.JWT_SECRET = orig;
});
```

- [ ] **Step 3: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`Cannot find module '../src/config'`。

- [ ] **Step 4: 写最小实现**

`Write` 工具创建 `d:\项目\简历app\backend\src\config\index.js`：

```js
const dotenv = require('dotenv');
const path = require('path');

// 强制从 backend 根目录加载 .env
dotenv.config({ path: path.join(__dirname, '..', '..', '.env') });

const REQUIRED = [
  'PORT', 'WX_APPID', 'WX_SECRET', 'JWT_SECRET',
  'DB_HOST', 'DB_USER', 'DB_PASSWORD', 'DB_NAME',
  'REDIS_HOST', 'REDIS_PASSWORD',
  'DEEPSEEK_API_KEY', 'DEEPSEEK_BASE_URL', 'DEEPSEEK_MODEL',
];

function load() {
  const missing = REQUIRED.filter((k) => !process.env[k]);
  if (missing.length) {
    throw new Error(`Missing required env: ${missing.join(', ')}`);
  }
  return {
    NODE_ENV: process.env.NODE_ENV || 'development',
    PORT: parseInt(process.env.PORT, 10),
    WX_APPID: process.env.WX_APPID,
    WX_SECRET: process.env.WX_SECRET,
    JWT_SECRET: process.env.JWT_SECRET,
    JWT_EXPIRES_IN: process.env.JWT_EXPIRES_IN || '30d',
    DB: {
      host: process.env.DB_HOST,
      port: parseInt(process.env.DB_PORT, 10) || 3306,
      user: process.env.DB_USER,
      password: process.env.DB_PASSWORD,
      database: process.env.DB_NAME,
    },
    REDIS: {
      host: process.env.REDIS_HOST,
      port: parseInt(process.env.REDIS_PORT, 10) || 6379,
      password: process.env.REDIS_PASSWORD,
    },
    DEEPSEEK: {
      apiKey: process.env.DEEPSEEK_API_KEY,
      baseURL: process.env.DEEPSEEK_BASE_URL,
      model: process.env.DEEPSEEK_MODEL,
    },
    LOG_LEVEL: process.env.LOG_LEVEL || 'info',
  };
}

module.exports = load();
```

- [ ] **Step 5: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
# 先建 .env
cp .env.example .env
# 改 .env 里的密码为真实值
npm test
```

预期：PASS，2 tests passing。

如果 FAIL：检查 `.env` 是否所有 REQUIRED 字段都填了。

- [ ] **Step 6: commit**

```bash
cd /d/项目/简历app
git add backend/src/config/ backend/tests/config.test.js backend/.env.example
git commit -m "feat(config): env loader with required-key validation"
```

---

### 任务 1.3 — winston logger

**Files:**

- Create: `d:\项目\简历app\backend\src\utils\logger.js`
- Create: `d:\项目\简历app\backend\tests\logger.test.js`

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\logger.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const logger = require('../src/utils/logger');

test('logger has info method', () => {
  assert.equal(typeof logger.info, 'function');
});

test('logger has error method', () => {
  assert.equal(typeof logger.error, 'function');
});

test('logger respects level', () => {
  assert.ok(logger.level, 'level should be set');
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`Cannot find module '../src/utils/logger'`。

- [ ] **Step 3: 写最小实现**

`Write` 工具创建 `d:\项目\简历app\backend\src\utils\logger.js`：

```js
const winston = require('winston');
const config = require('../config');

const logger = winston.createLogger({
  level: config.LOG_LEVEL,
  format: winston.format.combine(
    winston.format.timestamp(),
    winston.format.json()
  ),
  transports: [
    new winston.transports.Console({
      format: winston.format.combine(
        winston.format.colorize(),
        winston.format.simple()
      ),
    }),
  ],
});

module.exports = logger;
```

- [ ] **Step 4: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：PASS，5 tests passing（config 2 + logger 3）。

- [ ] **Step 5: commit**

```bash
cd /d/项目/简历app
git add backend/src/utils/logger.js backend/tests/logger.test.js
git commit -m "feat(logger): winston json logger"
```

---

### 任务 1.4 — Express app 工厂 + health 路由（TDD 完整示例）

**Files:**

- Create: `d:\项目\简历app\backend\src\routes\health.js`
- Create: `d:\项目\简历app\backend\src\middleware\errorHandler.js`
- Create: `d:\项目\简历app\backend\src\app.js`
- Create: `d:\项目\简历app\backend\tests\health.test.js`

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\health.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const request = require('supertest');
const { createApp } = require('../src/app');

test('GET /api/health returns 200 with status ok', async () => {
  const app = createApp();
  const res = await request(app).get('/api/health');
  assert.equal(res.status, 200);
  assert.equal(res.body.code, 0);
  assert.equal(res.body.data.status, 'ok');
  assert.ok(res.body.data.timestamp, 'should include timestamp');
});

test('GET /api/health on unknown route returns 404', async () => {
  const app = createApp();
  const res = await request(app).get('/api/nonexistent');
  assert.equal(res.status, 404);
  assert.equal(res.body.code, 1404);
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`Cannot find module '../src/app'`。

- [ ] **Step 3: 写 health 路由**

`Write` 工具创建 `d:\项目\简历app\backend\src\routes\health.js`：

```js
const express = require('express');
const router = express.Router();

router.get('/', (req, res) => {
  res.json({
    code: 0,
    data: {
      status: 'ok',
      timestamp: new Date().toISOString(),
      uptime: process.uptime(),
    },
  });
});

module.exports = router;
```

- [ ] **Step 4: 写 error handler**

`Write` 工具创建 `d:\项目\简历app\backend\src\middleware\errorHandler.js`：

```js
const logger = require('../utils/logger');

class AppError extends Error {
  constructor(code, message, status = 400) {
    super(message);
    this.code = code;
    this.status = status;
  }
}

function notFoundHandler(req, res, next) {
  res.status(404).json({ code: 1404, message: `Not found: ${req.method} ${req.path}` });
}

function errorHandler(err, req, res, next) {
  logger.error({ err, path: req.path, method: req.method }, 'request error');

  if (err instanceof AppError) {
    return res.status(err.status).json({ code: err.code, message: err.message });
  }

  res.status(500).json({ code: 1500, message: 'Internal server error' });
}

module.exports = { AppError, notFoundHandler, errorHandler };
```

- [ ] **Step 5: 写 app 工厂**

`Write` 工具创建 `d:\项目\简历app\backend\src\app.js`：

```js
const express = require('express');
const healthRouter = require('./routes/health');
const { notFoundHandler, errorHandler } = require('./middleware/errorHandler');

function createApp() {
  const app = express();

  app.use(express.json({ limit: '1mb' }));

  app.use('/api/health', healthRouter);

  app.use(notFoundHandler);
  app.use(errorHandler);

  return app;
}

module.exports = { createApp };
```

- [ ] **Step 6: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：PASS，7 tests passing（config 2 + logger 3 + health 2）。

- [ ] **Step 7: commit**

```bash
cd /d/项目/简历app
git add backend/src/app.js backend/src/routes/health.js backend/src/middleware/errorHandler.js backend/tests/health.test.js
git commit -m "feat(app): express factory + health route + error handler"
```

---

### 任务 1.5 — 入口文件 + 端口监听

**Files:**

- Create: `d:\项目\简历app\backend\src\index.js`

**步骤：**

- [ ] **Step 1: 写入口**

`Write` 工具创建 `d:\项目\简历app\backend\src\index.js`：

```js
const { createApp } = require('./app');
const config = require('./config');
const logger = require('./utils/logger');

const app = createApp();

const server = app.listen(config.PORT, () => {
  logger.info({ port: config.PORT, env: config.NODE_ENV }, 'server started');
});

function shutdown(signal) {
  logger.info({ signal }, 'shutting down');
  server.close(() => {
    logger.info('http server closed');
    process.exit(0);
  });
  // 10s 强制退出
  setTimeout(() => process.exit(1), 10000);
}

process.on('SIGTERM', () => shutdown('SIGTERM'));
process.on('SIGINT', () => shutdown('SIGINT'));
```

- [ ] **Step 2: 手动启动验证**

```bash
cd /d/项目/简历app/backend
node src/index.js
```

预期：日志输出 `{"port":3000,"env":"development","level":"info","message":"server started"}`。

另开一个终端：

```bash
curl http://localhost:3000/api/health
```

预期：返 `{"code":0,"data":{"status":"ok",...}}`。

`Ctrl+C` 停止。

- [ ] **Step 3: commit**

```bash
cd /d/项目/简历app
git add backend/src/index.js
git commit -m "feat: server entry + graceful shutdown"
```

---

### 任务 1.6 — MySQL 连接池

**Files:**

- Create: `d:\项目\简历app\backend\src\config\db.js`
- Create: `d:\项目\简历app\backend\tests\db.test.js`

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\db.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const pool = require('../src/config/db');

test('db pool can query', async () => {
  const [rows] = await pool.query('SELECT 1 AS n');
  assert.equal(rows[0].n, 1);
});

test.after(async () => {
  await pool.end();
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`Cannot find module '../src/config/db'`。

- [ ] **Step 3: 写 db 连接池**

`Write` 工具创建 `d:\项目\简历app\backend\src\config\db.js`：

```js
const mysql = require('mysql2/promise');
const config = require('./index');

const pool = mysql.createPool({
  host: config.DB.host,
  port: config.DB.port,
  user: config.DB.user,
  password: config.DB.password,
  database: config.DB.database,
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
  charset: 'utf8mb4',
  dateStrings: false,
});

module.exports = pool;
```

- [ ] **Step 4: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：PASS，8 tests passing。

如果 FAIL `ECONNREFUSED`：检查 `.env` 里 `DB_HOST` 是不是 `127.0.0.1`，密码对不对，MySQL 有没有跑（`systemctl status mysql`）。

- [ ] **Step 5: commit**

```bash
cd /d/项目/简历app
git add backend/src/config/db.js backend/tests/db.test.js
git commit -m "feat(db): mysql2 connection pool"
```

---

### 任务 1.7 — Redis 客户端

**Files:**

- Create: `d:\项目\简历app\backend\src\config\redis.js`
- Create: `d:\项目\简历app\backend\tests\redis.test.js`

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\redis.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const redis = require('../src/config/redis');

test('redis can set and get', async () => {
  await redis.set('test:hello', 'world', 'EX', 10);
  const v = await redis.get('test:hello');
  assert.equal(v, 'world');
  await redis.del('test:hello');
});

test.after(async () => {
  await redis.quit();
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`Cannot find module '../src/config/redis'`。

- [ ] **Step 3: 写 redis 客户端**

`Write` 工具创建 `d:\项目\简历app\backend\src\config\redis.js`：

```js
const Redis = require('ioredis');
const config = require('./index');

const redis = new Redis({
  host: config.REDIS.host,
  port: config.REDIS.port,
  password: config.REDIS.password,
  maxRetriesPerRequest: 3,
  enableReadyCheck: true,
  lazyConnect: false,
});

redis.on('error', (err) => {
  // eslint-disable-next-line no-console
  console.error('[redis] error:', err.message);
});

module.exports = redis;
```

- [ ] **Step 4: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：PASS，9 tests passing。

如果 FAIL `NOAUTH`：检查 `.env` 里 `REDIS_PASSWORD` 对不对。

- [ ] **Step 5: commit**

```bash
cd /d/项目/简历app
git add backend/src/config/redis.js backend/tests/redis.test.js
git commit -m "feat(redis): ioredis client"
```

---

### 任务 1.8 — 建表 SQL（schema.sql）

**Files:**

- Create: `d:\项目\简历app\backend\src\db\schema.sql`

**步骤：**

- [ ] **Step 1: 写 schema.sql**

`Write` 工具创建 `d:\项目\简历app\backend\src\db\schema.sql`：

```sql
-- 简历推荐小程序数据库结构
-- MySQL 8.0+, utf8mb4

SET NAMES utf8mb4;
SET FOREIGN_KEY_CHECKS = 0;

-- 1. 用户表
CREATE TABLE IF NOT EXISTS `users` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `openid` VARCHAR(64) NOT NULL COMMENT '微信 openid',
  `unionid` VARCHAR(64) DEFAULT NULL COMMENT '微信 unionid',
  `nickname` VARCHAR(64) DEFAULT NULL COMMENT '昵称（前端可改）',
  `avatar_url` VARCHAR(512) DEFAULT NULL,
  `is_admin` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '冗余字段，权威在 admins 表',
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `last_login_at` TIMESTAMP NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_openid` (`openid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户';

-- 2. 简历表
CREATE TABLE IF NOT EXISTS `resumes` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `content_md` MEDIUMTEXT NOT NULL COMMENT 'LLM 生成的 Markdown',
  `content_json` JSON DEFAULT NULL COMMENT '结构化字段（阶段 3 用）',
  `source_form` JSON NOT NULL COMMENT '用户原始表单',
  `is_active` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '业务层唯一活动简历',
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_active` (`user_id`, `is_active`),
  KEY `idx_user_created` (`user_id`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='简历历史';

-- 3. 岗位表
CREATE TABLE IF NOT EXISTS `jobs` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `title` VARCHAR(128) NOT NULL COMMENT '岗位名',
  `company` VARCHAR(128) NOT NULL COMMENT '公司名',
  `city` VARCHAR(64) NOT NULL,
  `salary_min` INT UNSIGNED NOT NULL COMMENT 'K/月',
  `salary_max` INT UNSIGNED NOT NULL COMMENT 'K/月',
  `degree_required` VARCHAR(16) NOT NULL DEFAULT '不限' COMMENT '不限/大专/本科/硕士/博士',
  `experience_required` VARCHAR(16) NOT NULL DEFAULT '不限' COMMENT '不限/应届/1-3年/3-5年/5+年',
  `skills_required` JSON NOT NULL COMMENT '技能标签数组',
  `description_md` MEDIUMTEXT NOT NULL COMMENT '岗位描述',
  `is_online` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '上架开关',
  `is_deleted` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '软删',
  `sort_weight` INT NOT NULL DEFAULT 0 COMMENT '运营干预权重',
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_online_city` (`is_online`, `is_deleted`, `city`),
  KEY `idx_salary` (`salary_min`, `salary_max`),
  KEY `idx_degree` (`degree_required`),
  KEY `idx_experience` (`experience_required`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='岗位';

-- 4. 匹配历史表（永不删除）
CREATE TABLE IF NOT EXISTS `matches` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `resume_id` BIGINT UNSIGNED NOT NULL,
  `job_id` BIGINT UNSIGNED NOT NULL,
  `match_batch_id` VARCHAR(64) NOT NULL COMMENT '同一次匹配的批次',
  `score` TINYINT UNSIGNED NOT NULL COMMENT '0-100',
  `reason` VARCHAR(255) NOT NULL DEFAULT '' COMMENT '一句话理由',
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_resume_batch` (`user_id`, `resume_id`, `match_batch_id`),
  KEY `idx_user_created` (`user_id`, `created_at`),
  KEY `idx_batch_score` (`match_batch_id`, `score`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='匹配历史';

-- 5. 管理员表
CREATE TABLE IF NOT EXISTS `admins` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `openid` VARCHAR(64) NOT NULL,
  `note` VARCHAR(128) DEFAULT NULL COMMENT '备注',
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_openid` (`openid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='管理员白名单';

-- 6. Prompt 模板表
CREATE TABLE IF NOT EXISTS `prompts` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `code` VARCHAR(64) NOT NULL COMMENT 'resume_generate / match_rerank',
  `name` VARCHAR(128) NOT NULL,
  `content` MEDIUMTEXT NOT NULL,
  `version` INT UNSIGNED NOT NULL DEFAULT 1,
  `is_active` TINYINT(1) NOT NULL DEFAULT 1,
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code_active` (`code`, `is_active`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='Prompt 模板';

-- 7. 管理操作日志
CREATE TABLE IF NOT EXISTS `admin_operation_logs` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `admin_openid` VARCHAR(64) NOT NULL,
  `action` VARCHAR(64) NOT NULL COMMENT 'job.create / job.update / job.delete / prompt.update ...',
  `target_type` VARCHAR(32) DEFAULT NULL COMMENT 'job / prompt',
  `target_id` VARCHAR(64) DEFAULT NULL,
  `detail` JSON DEFAULT NULL,
  `ip` VARCHAR(64) DEFAULT NULL,
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_admin_time` (`admin_openid`, `created_at`),
  KEY `idx_action_time` (`action`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='管理操作日志';

SET FOREIGN_KEY_CHECKS = 1;
```

- [ ] **Step 2: 在服务器执行 SQL**

SSH 登录服务器（任务 0.1.2 装的）：

```bash
mysql -u root -p < /path/to/your/local/backend/src/db/schema.sql
```

把本地 `schema.sql` 先 scp 到服务器（或者用 `cat` 复制粘贴）。

- [ ] **Step 3: 验证 7 张表**

```bash
mysql -u root -p -e "USE resume_app; SHOW TABLES;"
```

预期：输出 7 张表：`admins, admin_operation_logs, jobs, matches, prompts, resumes, users`。

- [ ] **Step 4: commit（只 commit SQL 文件，远程执行）**

```bash
cd /d/项目/简历app
git add backend/src/db/schema.sql
git commit -m "feat(db): schema for 7 tables"
```

---

### 任务 1.9 — 种子数据 SQL（seed.sql）

**Files:**

- Create: `d:\项目\简历app\backend\src\db\seed.sql`

**步骤：**

- [ ] **Step 1: 写 seed.sql（精简版，20 岗位）**

`Write` 工具创建 `d:\项目\简历app\backend\src\db\seed.sql`：

```sql
-- 种子数据：20 岗位 + 2 prompt + 1 默认管理员
USE resume_app;

-- 默认管理员（先随便填一个 openid，等阶段 4 你自己登录后拿真实 openid 替换）
INSERT INTO `admins` (`openid`, `note`) VALUES
('REPLACE_WITH_YOUR_OPENID', '默认管理员，请在管理后台替换')
ON DUPLICATE KEY UPDATE `note` = VALUES(`note`);

-- Prompt 模板：简历生成
INSERT INTO `prompts` (`code`, `name`, `content`, `version`, `is_active`) VALUES
('resume_generate', '简历生成 Prompt（默认）',
'# 角色\n你是一位资深 HR，专长把零散经历改写成有冲击力的简历段落。\n\n# 任务\n根据用户提供的资料，生成一份结构化中文简历，输出 Markdown。\n\n# 用户资料\n{user_form}\n\n# 输出格式（严格遵守）\n```markdown\n# {{姓名}}\n\n## 个人概况\n- 期望城市：...\n- 期望岗位：...\n- 期望薪资：...K/月\n\n## 教育背景\n...\n\n## 工作经历\n...\n\n## 技能清单\n...\n\n## 项目亮点\n...\n```\n\n# 约束\n- 篇幅 ≤ 600 字\n- 用动词开头，避免空洞形容\n- 技能点必须从用户资料里出现，不要编造', 1, 1),

('match_rerank', '岗位匹配精排 Prompt（默认）',
'# 角色\n你是一位严格的求职顾问，评估候选人与岗位的匹配度。\n\n# 候选人简历\n{resume}\n\n# 候选岗位列表（JSON）\n{jobs}\n\n# 输出格式（严格 JSON，无多余文字）\n```json\n{\n  "results": [\n    {"job_id": 1, "score": 85, "reason": "技能 5/5 命中，3 年经验匹配本科要求"}\n  ]\n}\n```\n\n# 规则\n- 只评 list 里的岗位，不要新增\n- score 范围 0-100，60 以下说明不推荐\n- reason 一句话，≤ 30 字\n- 严格 JSON 输出，不要 markdown 代码块包裹', 1, 1)
ON DUPLICATE KEY UPDATE `content` = VALUES(`content`);

-- 20 条岗位种子（精简版，全在一线/新一线城市）
INSERT INTO `jobs` (`title`, `company`, `city`, `salary_min`, `salary_max`, `degree_required`, `experience_required`, `skills_required`, `description_md`, `is_online`, `is_deleted`, `sort_weight`) VALUES
('前端工程师', '示例科技 A', '北京', 15, 25, '本科', '1-3年', JSON_ARRAY('JavaScript', 'Vue', 'React'), '负责公司核心产品前端开发，参与需求评审。', 1, 0, 0),
('后端工程师', '示例科技 A', '北京', 18, 30, '本科', '1-3年', JSON_ARRAY('Node.js', 'MySQL', 'Redis'), '负责 API 设计与实现，保证服务稳定性。', 1, 0, 0),
('全栈工程师', '示例科技 B', '上海', 20, 35, '本科', '3-5年', JSON_ARRAY('Node.js', 'React', 'PostgreSQL'), '独立负责中小项目全栈开发。', 1, 0, 10),
('数据分析师', '示例科技 C', '深圳', 12, 20, '本科', '应届', JSON_ARRAY('SQL', 'Python', 'Tableau'), '业务数据分析，输出周报。', 1, 0, 0),
('产品经理', '示例科技 C', '深圳', 15, 25, '本科', '3-5年', JSON_ARRAY('Axure', '用户调研', '数据分析'), '负责 B 端产品规划。', 1, 0, 0),
('UI 设计师', '示例设计 D', '杭州', 10, 18, '大专', '1-3年', JSON_ARRAY('Figma', 'Sketch', '动效'), '负责移动端 UI 设计。', 1, 0, 0),
('运营专员', '示例科技 E', '广州', 8, 12, '大专', '应届', JSON_ARRAY('文案', '社群运营', 'Excel'), '用户社群维护，活动策划。', 1, 0, 0),
('测试工程师', '示例科技 F', '北京', 12, 20, '本科', '1-3年', JSON_ARRAY('Selenium', 'Python', 'Postman'), '自动化测试脚本编写。', 1, 0, 0),
('DevOps 工程师', '示例科技 G', '上海', 20, 35, '本科', '3-5年', JSON_ARRAY('Docker', 'K8s', 'AWS'), 'CI/CD 流水线维护。', 1, 0, 5),
('算法工程师', '示例科技 H', '北京', 25, 40, '硕士', '1-3年', JSON_ARRAY('Python', 'PyTorch', 'NLP'), '推荐算法研发。', 1, 0, 0),
('iOS 开发', '示例科技 I', '深圳', 18, 30, '本科', '1-3年', JSON_ARRAY('Swift', 'Objective-C', 'iOS'), 'iOS App 开发与维护。', 1, 0, 0),
('Android 开发', '示例科技 I', '深圳', 18, 30, '本科', '1-3年', JSON_ARRAY('Kotlin', 'Java', 'Android'), 'Android App 开发与维护。', 1, 0, 0),
('市场营销', '示例科技 J', '上海', 10, 18, '本科', '1-3年', JSON_ARRAY('品牌推广', '文案', 'PPT'), '品牌营销方案策划。', 1, 0, 0),
('财务专员', '示例科技 K', '北京', 8, 15, '本科', '应届', JSON_ARRAY('Excel', '用友', '税务'), '日常账务处理。', 1, 0, 0),
('人力资源', '示例科技 K', '北京', 10, 18, '本科', '1-3年', JSON_ARRAY('招聘', '员工关系', '培训'), '负责招聘与员工关系。', 1, 0, 0),
('销售经理', '示例科技 L', '广州', 12, 25, '大专', '3-5年', JSON_ARRAY('客户开发', '谈判', 'CRM'), 'B 端客户开发与维护。', 1, 0, 0),
('内容编辑', '示例科技 M', '杭州', 8, 15, '本科', '应届', JSON_ARRAY('文案', '排版', '短视频'), '公众号内容生产。', 1, 0, 0),
('客服主管', '示例科技 N', '深圳', 10, 18, '大专', '3-5年', JSON_ARRAY('客服管理', '沟通', '培训'), '客服团队管理。', 1, 0, 0),
('Java 工程师', '示例科技 O', '上海', 18, 30, '本科', '3-5年', JSON_ARRAY('Java', 'Spring', 'MySQL'), '后端服务开发。', 1, 0, 0),
('架构师', '示例科技 P', '北京', 35, 60, '硕士', '5+年', JSON_ARRAY('架构设计', '分布式', '高并发'), '系统架构演进。', 1, 0, 0);
```

- [ ] **Step 2: 在服务器执行**

```bash
mysql -u root -p < /path/to/seed.sql
```

- [ ] **Step 3: 验证**

```bash
mysql -u root -p -e "USE resume_app; SELECT COUNT(*) AS job_count FROM jobs; SELECT COUNT(*) AS prompt_count FROM prompts;"
```

预期：job_count=20, prompt_count=2。

- [ ] **Step 4: commit**

```bash
cd /d/项目/简历app
git add backend/src/db/seed.sql
git commit -m "feat(db): seed 20 jobs + 2 prompts + 1 admin"
```

---

### 任务 1.10 — DB 初始化脚本（npm run db:init）

**Files:**

- Create: `d:\项目\简历app\backend\scripts\db-init.js`
- Create: `d:\项目\简历app\backend\tests\db-init.test.js`

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\db-init.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');

test('schema.sql file exists', () => {
  const p = path.join(__dirname, '..', 'src', 'db', 'schema.sql');
  assert.ok(fs.existsSync(p), 'schema.sql should exist');
});

test('seed.sql file exists', () => {
  const p = path.join(__dirname, '..', 'src', 'db', 'seed.sql');
  assert.ok(fs.existsSync(p), 'seed.sql should exist');
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL（之前 schema.sql/seed.sql 已建好，但 db-init.js 没建）。其实这次应该 PASS，因为文件已建。所以这个测试是冗余的，跳过 step 2。

- [ ] **Step 3: 写 db-init 脚本**

`Write` 工具创建 `d:\项目\简历app\backend\scripts\db-init.js`：

```js
#!/usr/bin/env node
/**
 * 一键初始化数据库：建表 + 灌种子数据
 * 用法：node scripts/db-init.js
 */
const fs = require('fs');
const path = require('path');
const mysql = require('mysql2/promise');
const config = require('../src/config');

async function run() {
  const schemaPath = path.join(__dirname, '..', 'src', 'db', 'schema.sql');
  const seedPath = path.join(__dirname, '..', 'src', 'db', 'seed.sql');

  const schema = fs.readFileSync(schemaPath, 'utf8');
  const seed = fs.readFileSync(seedPath, 'utf8');

  const conn = await mysql.createConnection({
    host: config.DB.host,
    port: config.DB.port,
    user: config.DB.user,
    password: config.DB.password,
    multipleStatements: true,
  });

  try {
    console.log('[db-init] running schema.sql...');
    await conn.query(schema);
    console.log('[db-init] schema applied');

    console.log('[db-init] running seed.sql...');
    await conn.query(seed);
    console.log('[db-init] seed applied');

    console.log('[db-init] done');
  } finally {
    await conn.end();
  }
}

run().catch((err) => {
  console.error('[db-init] failed:', err.message);
  process.exit(1);
});
```

- [ ] **Step 4: 本地试跑（如果本地有 MySQL）**

```bash
cd /d/项目/简历app/backend
npm run db:init
```

预期：日志输出 `[db-init] schema applied` + `[db-init] seed applied` + `[db-init] done`。

如果本地没 MySQL 跳过此步，部署到服务器后用 `mysql <` 命令即可。

- [ ] **Step 5: commit**

```bash
cd /d/项目/简历app
git add backend/scripts/db-init.js backend/tests/db-init.test.js
git commit -m "feat(db): one-click init script (schema + seed)"
```

---

### 任务 1.11 — 微信 code2session service

**Files:**

- Create: `d:\项目\简历app\backend\src\services\wechat.js`
- Create: `d:\项目\简历app\backend\tests\wechat.test.js`

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\wechat.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const { code2session } = require('../src/services/wechat');

test('code2session returns openid on success', async () => {
  // 拦截 axios，模拟微信返回
  const axios = require('axios');
  const orig = axios.get;
  axios.get = async (url) => {
    if (url.includes('/sns/jscode2session')) {
      return { data: { openid: 'mock_openid_123', session_key: 'mock_sk' } };
    }
    return orig(url);
  };

  const result = await code2session('mock_js_code');
  assert.equal(result.openid, 'mock_openid_123');
  assert.equal(result.session_key, 'mock_sk');

  axios.get = orig;
});

test('code2session throws on wechat error', async () => {
  const axios = require('axios');
  const orig = axios.get;
  axios.get = async () => ({ data: { errcode: 40029, errmsg: 'invalid code' } });

  await assert.rejects(
    () => code2session('bad_code'),
    /invalid code/
  );

  axios.get = orig;
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`Cannot find module '../src/services/wechat'`。

- [ ] **Step 3: 写 wechat service**

`Write` 工具创建 `d:\项目\简历app\backend\src\services\wechat.js`：

```js
const axios = require('axios');
const config = require('../config');
const { AppError } = require('../middleware/errorHandler');

const CODE2SESSION_URL =
  'https://api.weixin.qq.com/sns/jscode2session';

/**
 * 微信 code2session：用 code 换 openid + session_key
 * @param {string} code 小程序 wx.login() 返回的 code
 * @returns {Promise<{openid: string, session_key: string, unionid?: string}>}
 */
async function code2session(code) {
  if (!code || typeof code !== 'string') {
    throw new AppError(1000, 'code is required', 400);
  }

  const url = `${CODE2SESSION_URL}?appid=${config.WX_APPID}&secret=${config.WX_SECRET}&js_code=${code}&grant_type=authorization_code`;

  const { data } = await axios.get(url, { timeout: 5000 });

  if (data.errcode) {
    throw new AppError(1001, `wechat error: ${data.errmsg}`, 400);
  }

  return {
    openid: data.openid,
    session_key: data.session_key,
    unionid: data.unionid,
  };
}

module.exports = { code2session };
```

- [ ] **Step 4: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：PASS，13 tests passing。

- [ ] **Step 5: commit**

```bash
cd /d/项目/简历app
git add backend/src/services/wechat.js backend/tests/wechat.test.js
git commit -m "feat(auth): wechat code2session service"
```

---

### 任务 1.12 — JWT token 工具

**Files:**

- Create: `d:\项目\简历app\backend\src\services\token.js`
- Create: `d:\项目\简历app\backend\tests\token.test.js`

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\token.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const { sign, verify } = require('../src/services/token');

test('sign then verify returns same payload', () => {
  const payload = { userId: 1, openid: 'o123' };
  const token = sign(payload);
  const decoded = verify(token);
  assert.equal(decoded.userId, 1);
  assert.equal(decoded.openid, 'o123');
});

test('verify throws on invalid token', () => {
  assert.throws(() => verify('invalid.token.here'), /jwt/i);
});

test('verify throws on expired token', () => {
  const jwt = require('jsonwebtoken');
  const config = require('../src/config');
  const expired = jwt.sign({ userId: 1 }, config.JWT_SECRET, { expiresIn: '-1s' });
  assert.throws(() => verify(expired), /expired/i);
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`Cannot find module '../src/services/token'`。

- [ ] **Step 3: 写 token service**

`Write` 工具创建 `d:\项目\简历app\backend\src\services\token.js`：

```js
const jwt = require('jsonwebtoken');
const config = require('../config');
const { AppError } = require('../middleware/errorHandler');

function sign(payload) {
  return jwt.sign(payload, config.JWT_SECRET, {
    expiresIn: config.JWT_EXPIRES_IN,
  });
}

function verify(token) {
  try {
    return jwt.verify(token, config.JWT_SECRET);
  } catch (err) {
    if (err.name === 'TokenExpiredError') {
      throw new AppError(1002, 'token expired', 401);
    }
    throw new AppError(1002, 'invalid token', 401);
  }
}

module.exports = { sign, verify };
```

- [ ] **Step 4: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：PASS，16 tests passing。

- [ ] **Step 5: commit**

```bash
cd /d/项目/简历app
git add backend/src/services/token.js backend/tests/token.test.js
git commit -m "feat(auth): jwt sign + verify"
```

---

### 任务 1.13 — 用户鉴权中间件

**Files:**

- Create: `d:\项目\简历app\backend\src\middleware\auth.js`
- Create: `d:\项目\简历app\backend\tests\middleware-auth.test.js`

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\middleware-auth.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const express = require('express');
const request = require('supertest');
const { sign } = require('../src/services/token');
const { userAuth } = require('../src/middleware/auth');

function makeApp() {
  const app = express();
  app.get('/protected', userAuth, (req, res) => {
    res.json({ code: 0, data: { userId: req.user.userId } });
  });
  return app;
}

test('userAuth allows valid token', async () => {
  const token = sign({ userId: 42, openid: 'o42' });
  const res = await request(makeApp())
    .get('/protected')
    .set('Authorization', `Bearer ${token}`);
  assert.equal(res.status, 200);
  assert.equal(res.body.data.userId, 42);
});

test('userAuth rejects missing token', async () => {
  const res = await request(makeApp()).get('/protected');
  assert.equal(res.status, 401);
  assert.equal(res.body.code, 1002);
});

test('userAuth rejects bad token', async () => {
  const res = await request(makeApp())
    .get('/protected')
    .set('Authorization', 'Bearer garbage');
  assert.equal(res.status, 401);
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`Cannot find module '../src/middleware/auth'`。

- [ ] **Step 3: 写 auth 中间件**

`Write` 工具创建 `d:\项目\简历app\backend\src\middleware\auth.js`：

```js
const { verify } = require('../services/token');
const { AppError } = require('./errorHandler');

function userAuth(req, res, next) {
  const auth = req.headers.authorization;
  if (!auth || !auth.startsWith('Bearer ')) {
    return next(new AppError(1002, 'missing token', 401));
  }
  const token = auth.slice(7);
  try {
    const payload = verify(token);
    req.user = payload;
    next();
  } catch (err) {
    next(err);
  }
}

module.exports = { userAuth };
```

- [ ] **Step 4: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：PASS，19 tests passing。

- [ ] **Step 5: commit**

```bash
cd /d/项目/简历app
git add backend/src/middleware/auth.js backend/tests/middleware-auth.test.js
git commit -m "feat(auth): user auth middleware (Bearer token)"
```

---

### 任务 1.14 — 管理员鉴权中间件

**Files:**

- Create: `d:\项目\简历app\backend\src\middleware\adminAuth.js`
- Create: `d:\项目\简历app\backend\tests\middleware-admin-auth.test.js`

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\middleware-admin-auth.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const express = require('express');
const request = require('supertest');
const { userAuth } = require('../src/middleware/auth');
const { adminAuth } = require('../src/middleware/adminAuth');
const { sign } = require('../src/services/token');
const pool = require('../src/config/db');

function makeApp() {
  const app = express();
  app.get('/admin-only', userAuth, adminAuth, (req, res) => {
    res.json({ code: 0 });
  });
  return app;
}

test('adminAuth allows admin user', async () => {
  // 插一个临时管理员
  const openid = 'test_admin_openid_' + Date.now();
  await pool.query('INSERT INTO admins (openid, note) VALUES (?, ?)', [openid, 'test']);

  const token = sign({ userId: 999, openid });
  const res = await request(makeApp())
    .get('/admin-only')
    .set('Authorization', `Bearer ${token}`);

  assert.equal(res.status, 200);

  // 清理
  await pool.query('DELETE FROM admins WHERE openid = ?', [openid]);
});

test('adminAuth rejects non-admin user', async () => {
  const token = sign({ userId: 1, openid: 'not_admin_' + Date.now() });
  const res = await request(makeApp())
    .get('/admin-only')
    .set('Authorization', `Bearer ${token}`);
  assert.equal(res.status, 403);
  assert.equal(res.body.code, 1003);
});

test.after(async () => {
  await pool.end();
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`Cannot find module '../src/middleware/adminAuth'`。

- [ ] **Step 3: 写 adminAuth 中间件**

`Write` 工具创建 `d:\项目\简历app\backend\src\middleware\adminAuth.js`：

```js
const pool = require('../config/db');
const { AppError } = require('./errorHandler');

async function adminAuth(req, res, next) {
  try {
    const [rows] = await pool.query(
      'SELECT id FROM admins WHERE openid = ? LIMIT 1',
      [req.user.openid]
    );
    if (!rows.length) {
      return next(new AppError(1003, 'admin only', 403));
    }
    next();
  } catch (err) {
    next(err);
  }
}

module.exports = { adminAuth };
```

- [ ] **Step 4: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：PASS，21 tests passing。

- [ ] **Step 5: commit**

```bash
cd /d/项目/简历app
git add backend/src/middleware/adminAuth.js backend/tests/middleware-admin-auth.test.js
git commit -m "feat(auth): admin auth middleware (admins table check)"
```

---

### 任务 1.15 — POST /api/auth/login 路由

**Files:**

- Create: `d:\项目\简历app\backend\src\routes\auth.js`
- Modify: `d:\项目\简历app\backend\src\app.js` (加 auth router)

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\route-auth.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const request = require('supertest');
const { createApp } = require('../src/app');
const { code2session } = require('../src/services/wechat');
const pool = require('../src/config/db');

// mock code2session
const origCode2session = code2session;

test('POST /api/auth/login with valid code returns token + user', async () => {
  require('../src/services/wechat').code2session = async (code) => ({
    openid: 'test_openid_' + Date.now(),
    session_key: 'sk',
  });

  const openid = 'test_openid_for_cleanup';
  // 提前清理
  await pool.query('DELETE FROM users WHERE openid = ?', [openid]);

  const res = await request(createApp())
    .post('/api/auth/login')
    .send({ code: 'mock_code' });

  assert.equal(res.status, 200);
  assert.equal(res.body.code, 0);
  assert.ok(res.body.data.token, 'should return token');
  assert.ok(res.body.data.user.id, 'should return user with id');
  assert.equal(res.body.data.user.openid, openid);

  // 清理
  await pool.query('DELETE FROM users WHERE openid = ?', [openid]);
});

test('POST /api/auth/login with missing code returns 400', async () => {
  const res = await request(createApp())
    .post('/api/auth/login')
    .send({});
  assert.equal(res.status, 400);
  assert.equal(res.body.code, 1000);
});

test('POST /api/auth/login with wechat error returns 400', async () => {
  require('../src/services/wechat').code2session = async () => {
    const { AppError } = require('../src/middleware/errorHandler');
    throw new AppError(1001, 'invalid code', 400);
  };

  const res = await request(createApp())
    .post('/api/auth/login')
    .send({ code: 'bad' });

  assert.equal(res.status, 400);
  assert.equal(res.body.code, 1001);
});

test.after(async () => {
  require('../src/services/wechat').code2session = origCode2session;
  await pool.end();
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`/api/auth/login` 返 404。

- [ ] **Step 3: 写 auth 路由**

`Write` 工具创建 `d:\项目\简历app\backend\src\routes\auth.js`：

```js
const express = require('express');
const router = express.Router();
const { code2session } = require('../services/wechat');
const { sign } = require('../services/token');
const pool = require('../config/db');
const { AppError } = require('../middleware/errorHandler');
const logger = require('../utils/logger');

router.post('/login', async (req, res, next) => {
  try {
    const { code } = req.body;
    if (!code) {
      throw new AppError(1000, 'code is required', 400);
    }

    // 1. 微信 code2session
    const wx = await code2session(code);

    // 2. 查/插 user
    const [existing] = await pool.query(
      'SELECT id, openid, nickname, avatar_url FROM users WHERE openid = ? LIMIT 1',
      [wx.openid]
    );

    let user;
    if (existing.length) {
      user = existing[0];
      await pool.query(
        'UPDATE users SET last_login_at = NOW() WHERE id = ?',
        [user.id]
      );
    } else {
      const [result] = await pool.query(
        'INSERT INTO users (openid, last_login_at) VALUES (?, NOW())',
        [wx.openid]
      );
      user = { id: result.insertId, openid: wx.openid, nickname: null, avatar_url: null };
    }

    // 3. 签 token
    const token = sign({ userId: user.id, openid: user.openid });

    logger.info({ userId: user.id, openid: user.openid }, 'user login');

    res.json({ code: 0, data: { token, user } });
  } catch (err) {
    next(err);
  }
});

module.exports = router;
```

- [ ] **Step 4: 挂到 app**

修改 `d:\项目\简历app\backend\src\app.js` 的 `createApp` 函数，在 `app.use('/api/health', healthRouter);` 后面加一行：

```js
const authRouter = require('./routes/auth');
// ...
app.use('/api/auth', authRouter);
```

完整文件内容（替换原文件）：

```js
const express = require('express');
const healthRouter = require('./routes/health');
const authRouter = require('./routes/auth');
const { notFoundHandler, errorHandler } = require('./middleware/errorHandler');

function createApp() {
  const app = express();

  app.use(express.json({ limit: '1mb' }));

  app.use('/api/health', healthRouter);
  app.use('/api/auth', authRouter);

  app.use(notFoundHandler);
  app.use(errorHandler);

  return app;
}

module.exports = { createApp };
```

- [ ] **Step 5: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：PASS，24 tests passing。

- [ ] **Step 6: commit**

```bash
cd /d/项目/简历app
git add backend/src/routes/auth.js backend/src/app.js backend/tests/route-auth.test.js
git commit -m "feat(auth): POST /api/auth/login (code2session + upsert user + jwt)"
```

---

### 任务 1.16 — DeepSeek LLM 客户端

**Files:**

- Create: `d:\项目\简历app\backend\src\services\llm.js`
- Create: `d:\项目\简历app\backend\tests\llm.test.js`

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\llm.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const { chat, chatJson } = require('../src/services/llm');

test('chat returns content from deepseek', async () => {
  const axios = require('axios');
  const orig = axios.post;
  axios.post = async (url, body) => {
    if (url.includes('/chat/completions')) {
      return {
        data: {
          choices: [{ message: { content: 'mock reply' } }],
          usage: { prompt_tokens: 10, completion_tokens: 5, total_tokens: 15 },
        },
      };
    }
    return orig(url, body);
  };

  const result = await chat([{ role: 'user', content: 'hi' }]);
  assert.equal(result.content, 'mock reply');
  assert.equal(result.usage.total_tokens, 15);

  axios.post = orig;
});

test('chatJson parses JSON response', async () => {
  const axios = require('axios');
  const orig = axios.post;
  axios.post = async () => ({
    data: {
      choices: [{ message: { content: '{"score":85,"reason":"good"}' } }],
      usage: { total_tokens: 20 },
    },
  });

  const result = await chatJson([{ role: 'user', content: 'x' }]);
  assert.equal(result.parsed.score, 85);
  assert.equal(result.parsed.reason, 'good');

  axios.post = orig;
});

test('chatJson strips markdown code fences', async () => {
  const axios = require('axios');
  const orig = axios.post;
  axios.post = async () => ({
    data: {
      choices: [{ message: { content: '```json\n{"a":1}\n```' } }],
      usage: { total_tokens: 5 },
    },
  });

  const result = await chatJson([{ role: 'user', content: 'x' }]);
  assert.equal(result.parsed.a, 1);

  axios.post = orig;
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`Cannot find module '../src/services/llm'`。

- [ ] **Step 3: 写 llm service**

`Write` 工具创建 `d:\项目\简历app\backend\src\services\llm.js`：

```js
const axios = require('axios');
const config = require('../config');
const { AppError } = require('../middleware/errorHandler');

const CLIENT = axios.create({
  baseURL: config.DEEPSEEK.baseURL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${config.DEEPSEEK.apiKey}`,
  },
});

/**
 * 通用 chat 调用
 * @param {Array<{role:string,content:string}>} messages
 * @param {object} opts { model, maxTokens, temperature, responseFormat }
 * @returns {Promise<{content: string, usage: object}>}
 */
async function chat(messages, opts = {}) {
  try {
    const { data } = await CLIENT.post('/chat/completions', {
      model: opts.model || config.DEEPSEEK.model,
      messages,
      max_tokens: opts.maxTokens || 1000,
      temperature: opts.temperature ?? 0.7,
      ...(opts.responseFormat ? { response_format: opts.responseFormat } : {}),
    });
    return {
      content: data.choices[0].message.content,
      usage: data.usage,
    };
  } catch (err) {
    if (err.response) {
      throw new AppError(
        1100,
        `llm api error: ${err.response.data?.error?.message || err.message}`,
        502
      );
    }
    throw new AppError(1100, `llm network error: ${err.message}`, 502);
  }
}

/**
 * 强制 JSON 输出的 chat：会自动从 markdown code fence 提取 JSON
 */
async function chatJson(messages, opts = {}) {
  const result = await chat(messages, {
    ...opts,
    responseFormat: { type: 'json_object' },
  });

  let text = result.content.trim();
  // 去掉 markdown code fence
  text = text.replace(/^```(?:json)?\s*/i, '').replace(/\s*```$/, '');

  try {
    return { parsed: JSON.parse(text), usage: result.usage };
  } catch (err) {
    throw new AppError(1101, `llm returned invalid json: ${text.slice(0, 100)}`, 502);
  }
}

module.exports = { chat, chatJson };
```

- [ ] **Step 4: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：PASS，27 tests passing。

- [ ] **Step 5: commit**

```bash
cd /d/项目/简历app
git add backend/src/services/llm.js backend/tests/llm.test.js
git commit -m "feat(llm): deepseek client with chat + chatJson (auto fence strip)"
```

---

### 任务 1.17 — GET /api/test/llm 路由

**Files:**

- Create: `d:\项目\简历app\backend\src\routes\test.js`
- Modify: `d:\项目\简历app\backend\src\app.js` (挂 test router)

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\route-test-llm.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const request = require('supertest');
const { createApp } = require('../src/app');
const { chat } = require('../src/services/llm');

test('GET /api/test/llm returns ok with token info', async () => {
  const orig = chat;
  require('../src/services/llm').chat = async () => ({
    content: 'pong',
    usage: { prompt_tokens: 5, completion_tokens: 1, total_tokens: 6 },
  });

  const res = await request(createApp()).get('/api/test/llm');
  assert.equal(res.status, 200);
  assert.equal(res.body.code, 0);
  assert.equal(res.body.data.reply, 'pong');
  assert.equal(res.body.data.usage.total_tokens, 6);

  require('../src/services/llm').chat = orig;
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`/api/test/llm` 返 404。

- [ ] **Step 3: 写 test 路由**

`Write` 工具创建 `d:\项目\简历app\backend\src\routes\test.js`：

```js
const express = require('express');
const router = express.Router();
const { chat } = require('../services/llm');

router.get('/llm', async (req, res, next) => {
  try {
    const result = await chat(
      [{ role: 'user', content: '只回复 pong 一个词，不要任何其他内容' }],
      { maxTokens: 10, temperature: 0 }
    );
    res.json({
      code: 0,
      data: {
        reply: result.content,
        usage: result.usage,
        model: 'deepseek-chat',
      },
    });
  } catch (err) {
    next(err);
  }
});

module.exports = router;
```

- [ ] **Step 4: 挂到 app**

修改 `d:\项目\简历app\backend\src\app.js`，加 test router：

```js
const express = require('express');
const healthRouter = require('./routes/health');
const authRouter = require('./routes/auth');
const testRouter = require('./routes/test');
const { notFoundHandler, errorHandler } = require('./middleware/errorHandler');

function createApp() {
  const app = express();

  app.use(express.json({ limit: '1mb' }));

  app.use('/api/health', healthRouter);
  app.use('/api/auth', authRouter);
  app.use('/api/test', testRouter);

  app.use(notFoundHandler);
  app.use(errorHandler);

  return app;
}

module.exports = { createApp };
```

- [ ] **Step 5: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：PASS，28 tests passing。

- [ ] **Step 6: commit**

```bash
cd /d/项目/简历app
git add backend/src/routes/test.js backend/src/app.js backend/tests/route-test-llm.test.js
git commit -m "feat(llm): GET /api/test/llm endpoint"
```

---

### 任务 1.18 — 管理端路由占位（阶段 4 填）

**Files:**

- Create: `d:\项目\简历app\backend\src\routes\admin\index.js`
- Modify: `d:\项目\简历app\backend\src\app.js` (挂 admin router)

**步骤：**

- [ ] **Step 1: 写失败测试**

`Write` 工具创建 `d:\项目\简历app\backend\tests\route-admin-placeholder.test.js`：

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const request = require('supertest');
const { createApp } = require('../src/app');
const { sign } = require('../src/services/token');
const pool = require('../src/config/db');

test('GET /api/admin/check returns ok for admin', async () => {
  const openid = 'admin_check_test_' + Date.now();
  await pool.query('INSERT INTO admins (openid, note) VALUES (?, ?)', [openid, 'test']);

  const token = sign({ userId: 1, openid });
  const res = await request(createApp())
    .get('/api/admin/check')
    .set('Authorization', `Bearer ${token}`);

  assert.equal(res.status, 200);
  assert.equal(res.body.code, 0);
  assert.equal(res.body.data.isAdmin, true);

  await pool.query('DELETE FROM admins WHERE openid = ?', [openid]);
});

test('GET /api/admin/check returns 403 for non-admin', async () => {
  const token = sign({ userId: 1, openid: 'non_admin_' + Date.now() });
  const res = await request(createApp())
    .get('/api/admin/check')
    .set('Authorization', `Bearer ${token}`);

  assert.equal(res.status, 403);
});

test('GET /api/admin/check returns 401 without token', async () => {
  const res = await request(createApp()).get('/api/admin/check');
  assert.equal(res.status, 401);
});

test.after(async () => {
  await pool.end();
});
```

- [ ] **Step 2: 跑测试看红**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：FAIL，`/api/admin/check` 返 404。

- [ ] **Step 3: 写 admin 占位路由**

`Write` 工具创建 `d:\项目\简历app\backend\src\routes\admin\index.js`：

```js
const express = require('express');
const router = express.Router();
const { userAuth } = require('../../middleware/auth');
const { adminAuth } = require('../../middleware/adminAuth');

// 阶段 4 在此添加：job CRUD / prompt 读写 / 日志查询

router.get('/check', userAuth, adminAuth, (req, res) => {
  res.json({ code: 0, data: { isAdmin: true } });
});

module.exports = router;
```

- [ ] **Step 4: 挂到 app**

修改 `d:\项目\简历app\backend\src\app.js`：

```js
const express = require('express');
const healthRouter = require('./routes/health');
const authRouter = require('./routes/auth');
const testRouter = require('./routes/test');
const adminRouter = require('./routes/admin');
const { notFoundHandler, errorHandler } = require('./middleware/errorHandler');

function createApp() {
  const app = express();

  app.use(express.json({ limit: '1mb' }));

  app.use('/api/health', healthRouter);
  app.use('/api/auth', authRouter);
  app.use('/api/test', testRouter);
  app.use('/api/admin', adminRouter);

  app.use(notFoundHandler);
  app.use(errorHandler);

  return app;
}

module.exports = { createApp };
```

- [ ] **Step 5: 跑测试看绿**

```bash
cd /d/项目/简历app/backend
npm test
```

预期：PASS，31 tests passing。

- [ ] **Step 6: commit**

```bash
cd /d/项目/简历app
git add backend/src/routes/admin/ backend/src/app.js backend/tests/route-admin-placeholder.test.js
git commit -m "feat(admin): placeholder router + /api/admin/check"
```

---

### 任务 1.19 — PM2 配置

**Files:**

- Create: `d:\项目\简历app\backend\ecosystem.config.js`

**步骤：**

- [ ] **Step 1: 写 ecosystem.config.js**

`Write` 工具创建 `d:\项目\简历app\backend\ecosystem.config.js`：

```js
module.exports = {
  apps: [{
    name: 'resume-app-backend',
    script: './src/index.js',
    instances: 1,
    exec_mode: 'fork',
    max_memory_restart: '500M',
    env: {
      NODE_ENV: 'development',
    },
    env_production: {
      NODE_ENV: 'production',
    },
    error_file: './logs/err.log',
    out_file: './logs/out.log',
    merge_logs: true,
    time: true,
  }],
};
```

- [ ] **Step 2: 上传到服务器后启动**

（这一步需要服务器已部署代码，任务 1.20 会处理）

- [ ] **Step 3: commit**

```bash
cd /d/项目/简历app
git add backend/ecosystem.config.js
git commit -m "chore(pm2): ecosystem config"
```

---

### 任务 1.20 — Nginx 反代 + 自签证书

**Files:**

- Create: `d:\项目\简历app\deploy\nginx\resume-app.conf`

**步骤：**

- [ ] **Step 1: 写 nginx 站点配置**

`Write` 工具创建 `d:\项目\简历app\deploy\nginx\resume-app.conf`：

```nginx
# /etc/nginx/conf.d/resume-app.conf (部署时拷贝到服务器)
# 开发期：自签证书 + IP 访问

upstream resume_app_backend {
  server 127.0.0.1:3000;
  keepalive 32;
}

server {
  listen 80;
  server_name _;  # dev 期用 IP，备案后改域名

  # 强制跳 HTTPS
  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl;
  http2 on;
  server_name _;

  # 自签证书路径（部署时生成）
  ssl_certificate     /etc/nginx/ssl/resume-app.crt;
  ssl_certificate_key /etc/nginx/ssl/resume-app.key;

  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_ciphers HIGH:!aNULL:!MD5;

  # 通用安全头
  add_header X-Frame-Options DENY;
  add_header X-Content-Type-Options nosniff;
  add_header X-XSS-Protection "1; mode=block";

  # 客户端 body 上限
  client_max_body_size 2m;

  # API 反代
  location /api/ {
    proxy_pass http://resume_app_backend;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 35s;  # 略大于 LLM 35s 前端超时
  }

  # 健康检查单独暴露（不需走反代）
  location = /healthz {
    access_log off;
    return 200 "ok\n";
    add_header Content-Type text/plain;
  }
}
```

- [ ] **Step 2: 上传到服务器 + 装自签证书**

```bash
# 本地 git bash，把 nginx 配置 scp 上去
scp deploy/nginx/resume-app.conf root@123.45.67.89:/etc/nginx/conf.d/

# SSH 进服务器
ssh root@123.45.67.89

# 生成自签证书（IP 用你的服务器 IP）
mkdir -p /etc/nginx/ssl
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout /etc/nginx/ssl/resume-app.key \
  -out /etc/nginx/ssl/resume-app.crt \
  -subj "/C=CN/ST=Beijing/L=Beijing/O=Dev/CN=123.45.67.89"

# 测试配置 + reload
nginx -t
systemctl reload nginx
```

- [ ] **Step 3: 验证 HTTPS**

```bash
curl -k https://123.45.67.89/api/health
```

预期：返 `{"code":0,"data":{"status":"ok",...}}`（`-k` 跳过证书校验）。

- [ ] **Step 4: commit**

```bash
cd /d/项目/简历app
git add deploy/nginx/resume-app.conf
git commit -m "chore(nginx): dev mode config (self-signed cert + IP)"
```

---

### 任务 1.21 — 部署到服务器 + PM2 启动

**Files:** 无（运维操作）

**步骤：**

- [ ] **Step 1: 推送 develop 分支**

```bash
cd /d/项目/简历app
git push origin develop
```

- [ ] **Step 2: SSH 服务器 clone**

```bash
ssh root@123.45.67.89
mkdir -p /opt/resume-app
cd /opt/resume-app
git clone https://gitee.com/你的名字/resume-app.git .
git checkout develop
```

- [ ] **Step 3: 装依赖**

```bash
cd /opt/resume-app/backend
npm ci --production=false
cp .env.example .env
nano .env  # 改 DB/REDIS 密码为服务器上的真实值
```

- [ ] **Step 4: 跑建表 + 灌种子**

```bash
mysql -u root -p < src/db/schema.sql
mysql -u root -p < src/db/seed.sql
```

预期：两张 SQL 都成功执行。

- [ ] **Step 5: PM2 启动**

```bash
pm2 start ecosystem.config.js
pm2 status
pm2 logs --lines 50
```

预期：状态 `online`，日志无报错，看到 `server started`。

- [ ] **Step 6: 验证全部接口**

服务器本地测试：

```bash
curl http://127.0.0.1:3000/api/health
```

通过 Nginx HTTPS 测试：

```bash
curl -k https://123.45.67.89/api/health
curl -k https://123.45.67.89/api/test/llm
```

预期：都返 200 + `{"code":0,...}`。

- [ ] **Step 7: 设置 PM2 开机自启**

```bash
pm2 save
pm2 startup
# 按提示执行最后输出的命令（sudo env PATH=...）
```

---

## 阶段 1.5 — 缓冲 / 验收（1 个工作日）

阶段 1 跑通后，花 1 天做完整验收 + 修 P0/P1 bug。无新代码任务。

### 任务 1.5.1 — 全量验收清单

**Files:** 无

**步骤：**

- [ ] **Step 1: 跑测试套件**

```bash
cd /opt/resume-app/backend
npm test
```

预期：31 tests passing。

- [ ] **Step 2: 跑 Lint**

```bash
npm run lint
```

预期：0 error（warn 可接受）。

- [ ] **Step 3: 手动 smoke test**

服务器上跑：

```bash
# 1. health
curl http://127.0.0.1:3000/api/health

# 2. login（用真的 code 没意义，验 400）
curl -X POST http://127.0.0.1:3000/api/auth/login -H "Content-Type: application/json" -d '{}'

# 3. test-llm
curl http://127.0.0.1:3000/api/test/llm

# 4. 401
curl http://127.0.0.1:3000/api/admin/check

# 5. HTTPS
curl -k https://123.45.67.89/api/health
```

预期：1 返 ok / 2 返 400 code:1000 / 3 返 pong + token / 4 返 401 / 5 返 ok。

- [ ] **Step 4: 数据库抽查**

```bash
mysql -u root -p -e "USE resume_app; SELECT COUNT(*) FROM users; SELECT COUNT(*) FROM jobs; SELECT COUNT(*) FROM prompts; SELECT COUNT(*) FROM admins;"
```

预期：users 至少 1 条（测试时插的）/ jobs=20 / prompts=2 / admins=1。

- [ ] **Step 5: 写阶段 1 总结到 devlog**

```bash
cd /d/项目/简历app
cp devlog/template.md devlog/2026-06-28.md
# 填写今日目标 / 完成 / 踩坑 / 决策
```

- [ ] **Step 6: develop 集成无冲突**

```bash
git checkout develop
git pull origin develop
git log --oneline -10
```

预期：所有阶段 1 commit 在 develop 上，main 仍干净。

- [ ] **Step 7: 决定进入阶段 2 的条件**

如果上面 1-6 全过 + 0 个 P0 bug + LLM 真实调用（替换 mock 后）能跑通 → 阶段 1 通过。

---

## 阶段交付清单（阶段 0+1 全部完成后）

每阶段结束必产出：

- [ ] 所有 task 1.1 - 1.21 的代码 + 配置文件已 commit + push
- [ ] `devlog/2026-MM-DD.md` 当日日志已写（1.5 阶段）
- [ ] `docs/operations/{deploy,backup,monitoring}.md` 阶段 0 骨架存在
- [ ] 服务器 PM2 在线 + `/api/health` 返 200
- [ ] `/api/test/llm` 真连 DeepSeek 返 200
- [ ] Nginx HTTPS 反代验证通过
- [ ] MySQL 7 张表 + 20 岗位 + 2 prompt 落库
- [ ] 32+ tests 全过

阶段 2（小程序骨架）的启动条件：上述 8 项全过 + 决定用哪个小程序 UI 框架（原生 / TDesign / Vant）。
