# Phase 6 — 加固期 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** MVP 上线前加固：备份真演练 + MySQL 业务账号 + HTTPS 安全头 + 修 npm test hang。

**Architecture:**
- 服务器 ops：MySQL 创建业务账号 + Nginx 加 2 个 add_header + 独立 schema 备份演练
- 本地：6 个测试文件加 `test.after(async () => { ... await pool.end(); await redis.quit(); })` 让 npm test 自动退出
- 无新代码文件（全是 ops 配置 + 测试清理）

**Tech Stack:** MySQL 8.0 + Nginx 1.24 + Node 20 + node:test（无新依赖）

**前置文档**：[2026-06-29-简历推荐小程序-phase6-design.md](../specs/2026-06-29-简历推荐小程序-phase6-design.md)

---

## 文件改动清单

**服务器 ops**（不 push）：
- 服务器 MySQL：新建 `resume_app_user@'localhost'` + GRANT
- 服务器 `.env`：DB_USER + DB_PASSWORD
- 服务器 Nginx：`/etc/nginx/sites-enabled/resume-app.conf` 加 2 add_header

**本地测试**（push）：
- 6 个测试文件：补 `test.after` 的 `pool.end()` + `redis.quit()`

**文档**：
- `devlog/2026-06-29-backup-drill.md`（新建）
- `devlog/2026-06-29-phase6-verify.md`（新建）

---

## 任务列表（5 任务）

---

### Task 1: MySQL 业务账号 + restart backend

**目的**：避免 backend 用 root 连 MySQL。

- [ ] **Step 1: 生成强密码**

```bash
ssh -i "C:/Users/CRL/.ssh/id_r" ubuntu@43.139.176.199 'openssl rand -hex 16'
```

Expected: 输出 32 字符 hex（如 `a1b2c3d4e5f6...`）

**保存这个密码**（下一步用）。

- [ ] **Step 2: SSH 创建业务账号**

```bash
ssh -i "C:/Users/CRL/.ssh/id_r" ubuntu@43.139.176.199 << 'EOF'
NEW_PASS='<paste-from-step-1>'
mysql -u root -pResumeApp@2026 << SQL
CREATE USER 'resume_app_user'@'localhost' IDENTIFIED BY '$NEW_PASS';
GRANT SELECT, INSERT, UPDATE, DELETE ON resume_app.* TO 'resume_app_user'@'localhost';
FLUSH PRIVILEGES;
SQL
mysql -u root -pResumeApp@2026 -e "SELECT user, host FROM mysql.user WHERE user='resume_app_user';"
EOF
```

Expected: 输出 1 行 `resume_app_user  localhost`

- [ ] **Step 3: 修改服务器 .env**

```bash
ssh -i "C:/Users/CRL/.ssh/id_r" ubuntu@43.139.176.199 << EOF
sudo sed -i 's/^DB_USER=.*/DB_USER=resume_app_user/' /opt/resume-app/backend/.env
sudo sed -i "s|^DB_PASSWORD=.*|DB_PASSWORD=$NEW_PASS|" /opt/resume-app/backend/.env
grep '^DB_' /opt/resume-app/backend/.env
EOF
```

Expected:
```
DB_HOST=127.0.0.1
DB_USER=resume_app_user
DB_PASSWORD=<new password>
```

- [ ] **Step 4: Restart backend**

```bash
ssh -i "C:/Users/CRL/.ssh/id_r" ubuntu@43.139.176.199 'pm2 restart resume-app-backend --update-env && sleep 3 && pm2 status'
```

Expected: resume-app-backend online, restart count +1

- [ ] **Step 5: Smoke test（验证业务账号能用）**

```bash
curl -sk https://43.139.176.199/api/health
```

Expected:
```json
{"code":0,"message":"ok","data":{"status":"up","time":"..."}}
```

- [ ] **Step 6: 验证 root 不能从 backend 用**

```bash
# 用 root 直接 mysql -e 应能工作（admin 保留）
ssh -i "C:/Users/CRL/.ssh/id_r" ubuntu@43.139.176.199 "mysql -u root -pResumeApp@2026 -e 'SELECT 1'"
```

Expected: `1`

---

### Task 2: HTTPS 安全头（Nginx）

**Files:**
- Modify (server): `/etc/nginx/sites-enabled/resume-app.conf`

- [ ] **Step 1: Read current nginx config**

```bash
ssh -i "C:/Users/CRL/.ssh/id_r" ubuntu@43.139.176.199 'cat /etc/nginx/sites-enabled/resume-app.conf'
```

Expected: 输出当前 config（含 `add_header X-Frame-Options DENY;`）

- [ ] **Step 2: Add 2 headers via sed**

```bash
ssh -i "C:/Users/CRL/.ssh/id_r" ubuntu@43.139.176.199 << 'EOF'
sudo sed -i '/add_header X-Frame-Options DENY;/a\
  add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;\
  add_header Content-Security-Policy "default-src '\''self'\''; img-src '\''self'\'' data:; script-src '\''self'\'' '\''unsafe-inline'\'' '\''unsafe-eval'\''; style-src '\''self'\'' '\''unsafe-inline'\''; connect-src '\''self'\'' https://43.139.176.199;" always;' /etc/nginx/sites-enabled/resume-app.conf
EOF
```

- [ ] **Step 3: Verify config + reload**

```bash
ssh -i "C:/Users/CRL/.ssh/id_r" ubuntu@43.139.176.199 'sudo nginx -t && sudo nginx -s reload'
```

Expected: `syntax is ok` + `test successful`

- [ ] **Step 4: Verify headers**

```bash
curl -sk -I https://43.139.176.199/api/health | grep -iE 'strict-transport|content-security|x-frame'
```

Expected:
```
X-Frame-Options: DENY
Strict-Transport-Security: max-age=31536000; includeSubDomains
Content-Security-Policy: default-src 'self'; img-src 'self' data:; ...
```

---

### Task 3: 备份真演练（独立 schema）

- [ ] **Step 1: mysqldump + 记录生产行数**

```bash
ssh -i "C:/Users/CRL/.ssh/id_r" ubuntu@43.139.176.199 << 'EOF'
DUMP_FILE=/tmp/backup_drill_$(date +%Y%m%d_%H%M%S).sql
mysqldump -u root -pResumeApp@2026 --single-transaction --routines --triggers resume_app > $DUMP_FILE
ls -lh $DUMP_FILE
echo "---PROD COUNTS---"
mysql -u root -pResumeApp@2026 resume_app -e "
SELECT 'users' AS tbl, COUNT(*) AS cnt FROM users
UNION SELECT 'resumes', COUNT(*) FROM resumes
UNION SELECT 'jobs_online', COUNT(*) FROM jobs WHERE is_deleted=0
UNION SELECT 'matches', COUNT(*) FROM matches
UNION SELECT 'admins', COUNT(*) FROM admins
UNION SELECT 'prompts', COUNT(*) FROM prompts
UNION SELECT 'admin_logs', COUNT(*) FROM admin_operation_logs;" > /tmp/backup_drill_before.txt
cat /tmp/backup_drill_before.txt
EOF
```

Expected: SQL 文件大小显示 + 7 行表行数

- [ ] **Step 2: 创建独立 schema + 导入**

```bash
ssh -i "C:/Users/CRL/.ssh/id_r" ubuntu@43.139.176.199 << 'EOF'
DUMP_FILE=$(ls /tmp/backup_drill_*.sql | head -1)
mysql -u root -pResumeApp@2026 -e "CREATE DATABASE IF NOT EXISTS resume_app_test;"
mysql -u root -pResumeApp@2026 resume_app_test < $DUMP_FILE
echo "import done"
EOF
```

Expected: `import done`

- [ ] **Step 3: 验证恢复行数**

```bash
ssh -i "C:/Users/CRL/.ssh/id_r" ubuntu@43.139.176.199 << 'EOF'
mysql -u root -pResumeApp@2026 resume_app_test -e "
SELECT 'users' AS tbl, COUNT(*) AS cnt FROM users
UNION SELECT 'resumes', COUNT(*) FROM resumes
UNION SELECT 'jobs_online', COUNT(*) FROM jobs WHERE is_deleted=0
UNION SELECT 'matches', COUNT(*) FROM matches
UNION SELECT 'admins', COUNT(*) FROM admins
UNION SELECT 'prompts', COUNT(*) FROM prompts
UNION SELECT 'admin_logs', COUNT(*) FROM admin_operation_logs;" > /tmp/backup_drill_after.txt
diff /tmp/backup_drill_before.txt /tmp/backup_drill_after.txt && echo "DRILL OK"
EOF
```

Expected: `DRILL OK`（无 diff 输出）

- [ ] **Step 4: 清理**

```bash
ssh -i "C:/Users/CRL/.ssh/id_r" ubuntu@43.139.176.199 'mysql -u root -pResumeApp@2026 -e "DROP DATABASE resume_app_test;" && rm -f /tmp/backup_drill_*.sql /tmp/backup_drill_*.txt'
```

Expected: 无输出（成功）

- [ ] **Step 5: 写备份演练报告**

创建 `devlog/2026-06-29-backup-drill.md`：

```markdown
# 备份真演练 — 2026-06-29

## 演练摘要

| 项 | 数值 |
|----|------|
| 时间 | 2026-06-29 HH:MM |
| dump 文件大小 | X.XM |
| 恢复 schema | resume_app_test |
| 验证方式 | 7 张表 COUNT(*) diff |
| 结果 | ✅ DRILL OK |

## 演练命令

1. mysqldump
2. CREATE DATABASE resume_app_test
3. mysql < backup.sql
4. SELECT COUNT(*) 逐表对比
5. diff before.txt after.txt

## 7 张表行数

| 表 | 生产 | 恢复 | diff |
|----|------|------|------|
| users | X | X | 0 |
| resumes | X | X | 0 |
| jobs_online | X | X | 0 |
| matches | X | X | 0 |
| admins | X | X | 0 |
| prompts | X | X | 0 |
| admin_logs | X | X | 0 |

## 教训

（如有坑写这里，没有就写「无」）
```

填实际数值后 commit。

- [ ] **Step 6: Commit**

```bash
git add devlog/2026-06-29-backup-drill.md
git -c user.name='CRL' -c user.email='crl@crlcrl.com' commit -m "docs(devlog): Phase 6 backup drill report"
```

---

### Task 4: 修 npm test hang（6 文件加 test.after）

**Files:**
- Modify: `backend/tests/route-auth.test.js`
- Modify: `backend/tests/route-test-llm.test.js`
- Modify: `backend/tests/route-admin-placeholder.test.js`
- Modify: `backend/tests/resume-save.test.js`
- Modify: `backend/tests/resume-current.test.js`
- Modify: `backend/tests/health.test.js`

- [ ] **Step 1: route-auth.test.js**

读文件末尾，把现有 `test.after` 改成：

```js
test.after(async () => {
  require('../src/services/wechat').code2session = origCode2session;
  await pool.end();
  await require('../src/config/redis').quit();
});
```

- [ ] **Step 2: route-test-llm.test.js**

文件末尾追加（如已有 `pool.end()` 就不重复）：

```js
test.after(async () => {
  await pool.end();
  await require('../src/config/redis').quit();
});
```

- [ ] **Step 3: route-admin-placeholder.test.js**

同上追加。

- [ ] **Step 4: resume-save.test.js**

同上追加。

- [ ] **Step 5: resume-current.test.js**

同上追加。

- [ ] **Step 6: health.test.js**

末尾追加（health 没用 pool/redis，但加空 after 保持一致）：

```js
test.after(async () => {
  // health endpoint doesn't use pool/redis, but include for consistency
});
```

- [ ] **Step 7: 验证全量 npm test 不卡**

```bash
cd backend && time npm test
```

Expected: < 30s 完成，显示 N pass (0 fail)

- [ ] **Step 8: Commit**

```bash
git add backend/tests/route-auth.test.js backend/tests/route-test-llm.test.js backend/tests/route-admin-placeholder.test.js backend/tests/resume-save.test.js backend/tests/resume-current.test.js backend/tests/health.test.js
git -c user.name='CRL' -c user.email='crl@crlcrl.com' commit -m "test(backend): add pool.end() + redis.quit() to test.after hooks

Fixes npm test hang: HTTP integration tests left connections open,
causing node:test to wait for keep-alive sockets.

6 files touched:
- route-auth (already had pool.end, added redis.quit)
- route-test-llm (added both)
- route-admin-placeholder (added both)
- resume-save (added both)
- resume-current (added both)
- health (added empty for consistency)

Now npm test runs in <30s and exits cleanly."
```

---

### Task 5: 写 Phase 6 verify devlog

- [ ] **Step 1: 写 devlog**

创建 `devlog/2026-06-29-phase6-verify.md`：

```markdown
# 开发日志 — 2026-06-29（Phase 6 加固期）

> 阶段：6（上线打磨）
> 前置：[2026-06-29-phase5-verify.md](2026-06-29-phase5-verify.md)

## 今日目标

- [x] MySQL 业务账号（4 权限 GRANT）
- [x] HTTPS 安全头（HSTS + CSP + X-Frame-Options）
- [x] 备份真演练（独立 schema 验证）
- [x] npm test hang 修（6 文件 test.after）
- [x] Devlog

## 关键指标

| 项 | 数值 |
|----|------|
| MySQL 账号 | resume_app_user@'localhost' (4 权限) |
| HTTPS 头 | 3 个（X-Frame-Options 已有 + 新 HSTS + CSP）|
| 备份演练 | 7 张表行数 diff OK |
| npm test 时间 | < 30s（之前 60s+ 卡死）|
| 后端 commit | 1（test.after 修复）|
| devlog | 2 |

## 服务器 smoke 验证

| 项 | 结果 |
|----|------|
| backend health | ✅ 200 |
| nginx config | ✅ reload OK |
| curl -I headers | ✅ HSTS + CSP + X-Frame-Options |
| backup drill | ✅ DRILL OK |

## 决策记录

（从 spec §7 复制 5 条决策）

## 验收（来自 spec §1）

| # | 验收项 | 状态 |
|---|--------|------|
| 1 | 备份真演练 | ✅ |
| 2 | MySQL 业务账号 | ✅ |
| 3 | HTTPS 头 | ✅ |
| 4 | npm test | ✅ |
| 5 | 服务器 smoke | ✅ |
| 6 | Devlog | ✅ |

## Phase 7 启动清单

- [ ] 真机生产环境跑通（用户已注册 admin）
- [ ] 备案对接（如用 crlcrl.com）
- [ ] 准备微信审核材料（隐私协议/服务条款/截图）
- [ ] npm test 时间进入 CI（如果有的话）
```

填实际数值 + commit。

- [ ] **Step 2: Commit**

```bash
git add devlog/2026-06-29-phase6-verify.md
git -c user.name='CRL' -c user.email='crl@crlcrl.com' commit -m "docs(devlog): Phase 6 verification"
```

---

## 自审

**Spec 覆盖**：
- §2.1 MySQL 业务账号 → Task 1 ✅
- §2.2 HTTPS 头 → Task 2 ✅
- §2.3 备份演练 → Task 3 ✅
- §2.4 npm test 修 → Task 4 ✅
- §2.5 Devlog → Tasks 3 + 5 ✅

**Placeholder scan**：clean

**Type consistency**：
- `await require('../src/config/redis').quit()` 一致
- `await pool.end()` 一致

**修正**：
- spec §1 写"测试：≥ 6 新增（修 bug 算新增）"，实际 0 新测试 + 6 改 → 算"回归修复"不算新增。doc 说明清楚就行。

---

**Plan 完成**，保存到 `docs/superpowers/plans/2026-06-29-简历推荐小程序-phase6.md`（5 任务）。

执行选项：
1. **Subagent-Driven (推荐)** — 派 1 个 subagent 跑 5 任务（ops 性质，连续 SSH 操作）
2. **Inline Execution** — 我自己跑（subagent 在 ops 任务容易 SSH 卡）