<view class="container">
  <view class="card">
    <view style="font-size:48rpx; font-weight:bold;">简历助手</view>
    <view class="label" style="margin-top:16rpx;">AI 求职助手 6 步流程: 基本信息 → 画像 → 项目 → 匹配 → 简历 → 投递</view>
  </view>

  <view wx:if="{{hasResume}}" class="btn-secondary" bindtap="goPreview" style="margin-bottom:16rpx;">查看我的简历</view>
  <!-- R129 整合: 单一入口, 跳 jobpilot/index (6 步流) -->
  <view class="btn-primary" bindtap="goJobPilot">🚀 开始 AI 求职 ({{hasResume ? '继续' : '从基本信息开始'}})</view>
</view>
<privacy-popup id="privacy-popup"></privacy-popup>