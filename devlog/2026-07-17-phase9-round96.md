# 开发日志 — 2026-07-17（Phase 9 Round 96 — Bigscreen UI 精致化）

> 阶段：Phase 9 R96 — 大屏填简历 UX 升级
> 前置：[2026-07-16-phase9-round95.md](../devlog/2026-07-16-phase9-round95.md)

## 起点

R95 用户跑 bigscreen 5 步后反馈 "不够好看再优化一下". R96 加视觉精致度.

## 优化点 (8 项)

| # | 项 | 实现 |
|---|---|---|
| 1 | **Hero 区** | 绿渐变 (#07c160 → #00a854 → #008f4f) + 大标题 56rpx + 大图标 ✍️ + 装饰圆光晕 |
| 2 | **步骤指示器** | 浮在 hero 下方, 白卡 + 阴影, 圆点 + 连接线, 已过 = 绿实心 + ✓, 当前 = 放大 1.1x, 未过 = 灰空心 |
| 3 | **浮动 label 输入框** | focus 时 label 浮起到边框上 (top: -10rpx, font 22rpx), 主色; 已有值时也浮起 |
| 4 | **Chip 性别** | 大按钮 32rpx padding, active = 绿 + translateY(-2rpx) + 阴影 |
| 5 | **预览卡片** | 立体阴影 + 左侧 8rpx 绿竖条 + sticky 跟随滚动 + section title 绿方块装饰 |
| 6 | **完成度** | 大字号 56rpx 加粗, 紧贴 % 符号, 视觉冲击 |
| 7 | **统一色板** | 用 CSS variables (--primary / --text / --bg / --border 等), 全局一致 |
| 8 | **修 showLoading race** | timer 不再调 showLoading (避免配对警告) |

## 改了什么

| 文件 | 改动 |
|---|---|
| `mini-program/pages/form/bigscreen/bigscreen.wxss` | 重写: hero / 浮动 label / chip / preview sticky / 色板 vars |
| `mini-program/pages/form/bigscreen/bigscreen.wxml` | 重写: hero + stepper 浮卡 + 浮动 label 结构 + step-num-badge |
| `mini-program/pages/form/bigscreen/bigscreen.js` | submit timer 改 console.log (消除 showLoading 警告) |

## 关键代码

### Hero 区
```css
.hero {
  background: linear-gradient(135deg, #07c160 0%, #00a854 50%, #008f4f 100%);
  padding: 56rpx 48rpx 96rpx;
}
.hero::before {
  /* 右上角装饰圆光晕 */
  content: "";
  position: absolute;
  width: 400rpx; height: 400rpx;
  background: radial-gradient(circle, rgba(255,255,255,0.15) 0%, transparent 70%);
  border-radius: 50%;
}
```

### 步骤指示器
```css
.stepper {
  margin: -56rpx 32rpx 0;  /* 浮在 hero 下方 */
  background: white;
  border-radius: 32rpx;
  box-shadow: 0 4rpx 16rpx rgba(0,0,0,0.08);
}
.stepper-item.active .stepper-circle {
  transform: scale(1.1);  /* 当前步骤放大 */
  box-shadow: 0 4rpx 12rpx rgba(7,193,96,0.35);
}
```

### 浮动 label
```css
.field-float-label {
  position: absolute;
  top: 28rpx;
  transition: all 0.2s ease;
}
.field-float.has-value .field-float-label,
.field-float-input:focus + .field-float-label {
  top: -10rpx;
  font-size: 22rpx;
  color: var(--primary);
  background: var(--card);  /* 浮起时遮住边框线 */
}
```

### 预览卡片
```css
.preview-card {
  border-left: 8rpx solid var(--primary);  /* 左侧绿条 */
  position: sticky;
  top: 32rpx;
}
.preview-section-title::before {
  /* section 标题前绿方块 */
  content: "";
  width: 8rpx; height: 24rpx;
  background: var(--primary);
  border-radius: 4rpx;
}
```

## Verify

| 检查 | 结果 |
|---|---|
| `node --test tests/bigscreen.test.js` | ✅ 12/12 pass |
| Local bigscreen.wxss | 11.6KB (从 4.5KB) |
| Local bigscreen.wxml | 12.0KB (从 9.5KB) |
| Server sync | ✅ done |

## 留 follow-up

| # | 项 |
|---|---|
| 1 | 真机 verify 新视觉 (hero + 浮动 label + sticky preview) |
| 2 | 跑 5 步 → submit → preview verify |
| 3 | 进一步 polish: 步骤切换动画 (fade/slide) |
| 4 | 加 SVG 大圆环 完成度 (R96 我用大数字, 你想 SVG 环告诉我) |
| 5 | 完成度算法微调 |

## baseline

- mini-program: 12/12 (R95 不变)
- backend: 470+ pass
- 49 commits on develop (R40-R96)

## Commits

| SHA | msg |
|-----|-----|
| (本) | feat(mp): R96 — bigscreen UI 精致化 (hero + 浮动 label + sticky preview) |
| (本 devlog) | docs: R96 closeout |