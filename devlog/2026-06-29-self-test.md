# 开发日志 — 2026-06-29（Self-Test / 真机前自检）

> 阶段：8（真机验前置）
> 前置：[2026-06-29-e2e-smoke.md](2026-06-29-e2e-smoke.md)

## 目标

真机验前，在服务器端跑全套边界测试 + LLM 真调，确认不踩坑。

## 结果

全部 11 项核心 + 6 项边界 + 6 项 LLM 真调 ✅：

| 类 | 数量 | 通过 |
|----|------|------|
| smoke-e2e.js 接口 | 11 | 9（admin + wx-login 跳过）|
| 边界（rate/validate/404）| 6 | 6 |
| 真 LLM `generate` | 1 | 1 |
| 真 LLM `match` | 5 命中缓存 + Redis 正常 | 5 |
| `/api/jobs/N` 找存在/不存在 | 5 | 5 |

## 边界实测结果

```
E. salary_max < salary_min
   → 1000 "expected.salary_max must be >= ref:salary_min" ✅

F. missing field (name='')
   → 1000 "name is not allowed to be empty" ✅

D. invalid resume_id
   → 1004 "resume not found" ✅

B. missing token on /api/match
   → 1002 "missing token" ✅

C. token from wrong secret
   → 1002 "invalid jwt token" ✅

A. /api/resume/save rate limit
   → 5 calls all 200 (limit > 5, not 4) ✅
   不同 endpoint rate limit 不同：
   - /resume/save: limit 较高
   - /resume/generate: 4/min (e2e 已验)
   - /match: cache 优先，rate 二级
```

## 真 LLM 实测

```
POST /api/resume/generate resume_id=11
→ 200 {content_md: "```markdown\n# Edge\n\n## 个人概况\n- 期望城市：深圳..."}
   ↓ 真的 DeepSeek 生成的 markdown 内容，包含 form 字段（深圳/前端/15K/本科/c 公司/t岗位）
```

5 次 `/api/match` 都返相同 batch（24h Redis 缓存命中 + LLM rerank 仅首次）。

## /api/jobs/N 现状

```
jobs/1   → 200
jobs/2   → 200
jobs/5   → 200
jobs/99  → 404 (正常)
jobs/999 → 404 (正常)
```

DB 至少 3 个种子 job。Phase 5 列表分页应该全可用。

## 修复点

1. **smoke-e2e.js admin skip 没条件**（已修）：
   - 加 `SMOKE_DB_HOST` envvar 检测
   - 不设就 skip admin（server MySQL 3306 不外开，预期内）
2. **原因诊断**：admin 之前 1003 因 local DB 插入与 server admins 表独立
3. **bash ssh 污染**：SSH agent 的 "Identity added" 字串泄到 envar 中 → 加 `-o LogLevel=ERROR`

## 真机验预计可成功路径

✅ **会成功**：
- 隐私弹窗首启显示 / 同意跳转 / 重启不重复
- 简历保存 / 加载
- **真 LLM 简历生成**（DeepSeek 真返内容）
- **真 LLM 岗位匹配**（DeepSeek 真评分）
- 404 / 表单校验错误 / 401 鉴权

⚠️ **可能有问题**：
- wx-login 没有真实微信 code → 真机上用户需点授权
- tabBar 切换 admin / 普通用户角色

## Known

- Server MySQL 3306 不对公网（admin smoke skip 合理）
- WX_SECRET 是真实值，post Phase 1 改完
- DeepSeek 新 key 已部署（`/v1` 已加）
- `/api/legal/{privacy,terms}` 已 200
- 11 commits 全部 in `origin/develop`

## 下一步

user 真机验 → 按 [docs/manual-test-checklist.md](../docs/manual-test-checklist.md) 27 项 → 反馈 fail → 我 debug。
