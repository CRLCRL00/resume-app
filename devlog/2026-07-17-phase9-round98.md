# 开发日志 — 2026-07-17（Phase 9 Round 98 — 粒子星图填简历）

> 阶段：Phase 9 R98 — 用粒子星图可视化, 5 星座 (基本/教育/工作/期望/技能)
> 前置：[2026-07-17-phase9-round97.md](../devlog/2026-07-17-phase9-round97.md)

## 起点

R97 用户反馈 "想做成粒子星图让使用者点进去自己选". 推翻对话式, 用粒子可视化.

## 设计 — 粒子星图

### 视觉

- 深空背景 (`radial-gradient #0a0e1a → #020308`)
- 80 颗随机小背景星点 (装饰)
- 5 星座环绕中心 (5 个不同颜色 halo)
- 每个字段 = 1 颗发光粒子 (圆点 + glow)
- 中心节点 = 完成度圆 (pulse 动画)
- 浮动预览卡 (右下角, sticky)
- 弹窗 modal 填字段 (text / chips / picker)

### 5 星座颜色

| 星座 | 颜色 |
|---|---|
| 基本信息 | 蓝紫 #6366f1 |
| 教育 | 青绿 #06b6d4 |
| 工作 | 橙 #f59e0b |
| 期望 | 粉 #ec4899 |
| 技能 | 黄 #eab308 |

### 粒子布局

`layoutParticles(width, height)`:
- 5 星座均分 360° 环绕中心 (radius = min(W,H) * 0.32)
- 每星座内字段再均分小圆 (radius = 90)
- 已填字段 = 实心 + 强烈 glow + 更小动画周期

## 改了什么

| 文件 | 改动 |
|---|---|
| `mini-program/pages/form/bigscreen/bigscreen.js` | 重写: CONSTELLATIONS 5 个 + layoutParticles 计算位置 + 弹窗 modal save + onParticleTap 处理 |
| `mini-program/pages/form/bigscreen/bigscreen.wxml` | 重写: starfield + bg-star + 5 星座循环 + 中心完成度节点 + 浮动预览卡 + 弹窗 modal |
| `mini-program/pages/form/bigscreen/bigscreen.wxss` | 重写: 深空背景 + 粒子核心/光晕/动画 + 星座 halo + 浮动预览 + 弹窗 |
| `mini-program/tests/bigscreen.test.js` | + R98 验证 (CONSTELLATIONS / layoutParticles / genBackgroundStars / wxml 结构 / wxss 暗色) |

## 关键代码

### 粒子定位
```js
function layoutParticles(width, height) {
  return CONSTELLATIONS.map((c, i) => {
    const angle = (i * 72 - 90) * Math.PI / 180;
    const ccx = cx + Math.cos(angle) * orbitR;
    const ccy = cy + Math.sin(angle) * orbitR;
    const particles = c.fields.map((f, j) => ({
      ...f,
      x: ccx + Math.cos(partAngle) * partR,
      y: ccy + Math.sin(partAngle) * partR,
    }));
    return { ...c, cx: ccx, cy: ccy, particles };  // R98 fix: 用 ccx/ccy, 不是 cx/cy
  });
}
```

### 粒子样式 (发光)
```css
.particle-core {
  background: radial-gradient(circle, var(--pc) 0%, rgba(var(--pcrgb), 0.6) 100%);
  box-shadow: 0 0 16rpx var(--pc), inset 0 0 12rpx rgba(255,255,255,0.5);
  animation: twinkle 4s ease-in-out infinite;
}
.particle.filled .particle-core {
  background: radial-gradient(circle, #fff 0%, var(--pc) 60%);
  box-shadow: 0 0 32rpx var(--pc), 0 0 64rpx var(--pc);
}
```

### 弹窗 modal
```css
.modal-mask {
  background: rgba(0,0,0,0.7);
  backdrop-filter: blur(20rpx);
}
.modal-card {
  background: linear-gradient(180deg, #1a1f2e 0%, #0f1420 100%);
  border: 2rpx solid rgba(7,193,96,0.3);
  box-shadow: 0 12rpx 48rpx rgba(0,0,0,0.5);
}
```

## 修了一个 bug

**R98 第一次跑**: 5 星座 cx/cy 都返回 (375, 600) — 全在中心.
**Root cause**: `return { ...c, cx, cy, particles }` 把外层 `cx/cy` (中心) 覆盖了计算后的 `ccx/ccy`.
**Fix**: `return { ...c, cx: ccx, cy: ccy, particles }`.

## Verify

| 检查 | 结果 |
|---|---|
| `node --test tests/bigscreen.test.js` | ✅ 17/17 pass |
| Local sync | ✅ |
| Server sync | ✅ |

## 留 follow-up

| # | 项 |
|---|---|
| 1 | 真机 verify 粒子星图 (点粒子 → 弹窗 → 保存 → 星变亮) |
| 2 | 跑完 5 星座 14 粒子 → 100% → 点中心 → submit |
| 3 | 加连线 (filled 字段之间隐隐连线) |
| 4 | 加 Canvas 粒子层 (性能更好, 更多粒子) |
| 5 | 加背景音乐感动画 (粒子轻微 sin/cos 浮动) |

## baseline

- mini-program: 17/17 pass
- backend: 470+ pass
- 51 commits on develop (R40-R98)

## Commits

| SHA | msg |
|-----|-----|
| (本) | feat(mp): R98 — 粒子星图填简历 (5 星座 + 弹窗 + 深空背景) |
| (本 devlog) | docs: R98 closeout |